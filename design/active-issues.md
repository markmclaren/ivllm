# Active Issues

This document tracks known, pre-existing bugs in the `isambard-vllm` repository.

## Known Issues

- **`TieringOffloadingSpec` (the `OffloadingConnector` tiered KV-offload path referenced in `baseline_vllm_args()`'s TODO block) is single-node-only — three independent structural problems make 2-node TP deployments broken, and `cpu_bytes_to_use`/`--kv-offloading-size` are *group totals*, not per-worker. Research finding, 2026-09-05, from direct read of `vendor/vllm` (main) and `vendor/vllm-0.26.0` (both agree; code + official docs cited below).** No production code affected — the in-flight WIP change uses the **plain native path** (`--kv-offloading-backend native`), which is correct on both 1- and 2-node as detailed in (4). Findings:
  1. **`cpu_bytes_to_use` is the total host-memory budget across ALL workers in the TP group (world = TP×PP), not per GPU.** Official docs (`docs/features/kv_offloading_usage.md`, identical in `vllm-0.26.0`): *"Total bytes of host memory reserved for the CPU tier across all workers (not per-worker)"*. Code confirms (`vllm/v1/kv_offload/cpu/spec.py`, `CPUOffloadingSpec.__init__`, inherited unchanged by `TieredOffloadingSpec`): `kv_bytes_per_block = per_gpu_bytes_per_block × world_size` (one logical offload block = every rank's shard), then `num_blocks = cpu_bytes_to_use // aligned_total` and `cpu_page_size_per_worker = total // world_size`. The high-level CLI flag is the same budget in GiB — `CacheConfig.kv_offloading_size`'s own docstring: *"When TP > 1, this is the total buffer size summed across all TP ranks"* — and `vllm/config/vllm.py` converts it verbatim to `{"cpu_bytes_to_use": size × 2^30}` for the native backend. **Consequence for the TODO block in `utils.sh`**: its example `"cpu_bytes_to_use": 10737418240` would be 10 GiB for the *whole group* (2.5 GiB/GPU at TP=4), not 48 GiB/GPU — a tiered config matching the `tp*48` intent would need `tp × 48 × 2^30` bytes.
  2. **No NUMA binding anywhere in the offload code** (grep: zero numa/mbind/set_mempolicy/first-touch references across `vllm/v1/kv_offload/` and the offloading connectors). Placement is default-policy first-touch. Plain native path: each worker does `torch.zeros(..., pin_memory=True)` for its *own* buffer (`cpu/gpu_worker.py`) — NUMA-local *incidentally*, because srun `--cpus-per-gpu=72` + Ray actor affinity pins each worker to its GPU's 72-core NUMA range (the GLM CPU-allocation fix, verified via NCCL's per-GPU affinity resolution). Tiered path: `SharedOffloadRegion` mmaps `/dev/shm/vllm_offload_{instance_id}.mmap` and populates via `MADV_POPULATE_WRITE`, then the whole region is locked in place by `cudaHostRegister` (`gpu_worker.py:136`) — pages cannot migrate afterwards. The **scheduler-side** region (`TieredOffloadingSpec.get_manager()`, rank=None, runs in the EngineCore process) populates the **entire** file first, so page placement follows EngineCore's affinity rather than each worker's GPU. `/dev/shm` is one tmpfs **per node** (all 4 GH200s share it; default size 50% of RAM ≈ 240 GiB on a 480 GiB tray); all workers of a job on a node share the *same file* (first `O_EXCL` opener creates it, others attach) — the per-rank split is logical slices within block rows, not separate files. Note `ray-setup.sh` already sets `RAY_OBJECT_STORE_MEMORY=4 GiB`, avoiding Ray's 30%-of-RAM default object store (~144 GiB) which would have collided with a large offload region in the same tmpfs.
  3. **2-node TP=8 with the tiered path is broken in three independent ways** (verified against `vendor/vllm-0.26.0` + main): (a) each node's `/dev/shm` file is `ftruncate`d to the **full group budget** (`num_blocks × 8-rank aggregated row stride` = `cpu_bytes_to_use`), not half — and the head-node scheduler's eager full-region populate materializes **100% of the budget on the head node** (e.g. 384 GiB = 80% of node RAM at a `tp*48` scheme, and > the ~240 GiB default tmpfs limit → **ENOSPC during startup**, before serving); the non-head node only faults in its own 4 workers' slices, so even placement is asymmetric. (b) Slice addressing uses `torch.accelerator.current_device_index()` (`tiering/spec.py:191`) — the **local** device index (0–3 on *both* nodes; `RayExecutorV2` runs with `RAY_EXPERIMENTAL_NOSET_CUDA_VISIBLE_DEVICES` and `gpu_worker.init_device` sets the device from `local_rank`), not the global TP rank 0–7 — so the non-head node can never address slots 4–7 of its own file; those remain zero-filled dead space. (c) Secondary tiers (the `fs` tier in the TODO block) are constructed once, in the scheduler, with a zero-copy memoryview over the **head node's** file (`tiering/fs/manager.py` `store_block`/`load_block`); for a TP-sharded block, the shards owned by global ranks 4–7 live only in the non-head node's file and are **never visible to the fs tier** — stores would write rows with zero-filled slots 4–7 and loads would never populate the non-head local region: silent half-block corruption, surfacing as garbage completions, not an error.
  4. **The plain native path (the current WIP `--kv-offloading-size $((tp*48))`) is correct on both topologies**: per-worker anonymous pinned RAM (not /dev/shm), each worker holds 1/world of the budget, no shared state, NUMA-local first-touch — 192 GiB per node at TP=4, 192 GiB per node at 2-node TP=8 (384 GiB total). No /dev/shm involvement at all, so the tmpfs size question doesn't apply. **Conclusion**: benchmark the native path as-is; do not take the `TieringOffloadingSpec` path on multi-node TP without vLLM-side changes (per-node region sizing, global-rank slice addressing, per-node secondary tiers). Worth checking upstream (`vllm-project/vllm` issues/PRs on `TieringOffloadingSpec`/`OffloadingConnector` multi-node support) before investing in a local patch.

- **MiniMax-M3-AWQ-INT4 — RESOLVED, 2026-09-03: both `disable-flashinfer-import.v0.26.0.v2.patch` and `minimax-m3-indexer-unfuse.v0.26.0.v1.patch` confirmed working together on a real, complete run (`logs/mmax3q/20260903_213700/` and the run after it) — model loads, serves, and produces coherent responses.** History below kept for the full root-cause trail; this paragraph is the outcome. Two follow-ups opened from this same confirmation run, both separate from anything above:
  1. **Checkpoint-shard loading was very slow this run** (progressive per-shard slowdown, see the "Is marlin still the right choice" exchange below) — diagnosed as very likely transient shared-storage/node contention on Isambard (the first ~21 of 49 shards loaded at the same flat rate as a fully healthy prior run before degrading, which rules out a monotonic code-level regression) rather than anything wrong with either patch or with Marlin as the MoE kernel choice. Not a blocking issue — Rob confirmed "with patience it does get through it."
  2. **New, separate issue found on this successful run: thinking/reasoning blocks (`reasoning-parser: minimax_m3`, `<mm:think>...</mm:think>` tags per `vllm/reasoning/minimax_m3_reasoning_parser.py`) are not being parsed correctly, while tool-calling (`tool-call-parser: minimax_m3`) works fine.** Not yet investigated in depth — likely candidate given the parser source: `MiniMaxM3ReasoningParser`'s behavior branches on `chat_template_kwargs.get("thinking_mode") == "enabled"` (`_initial_in_reasoning`), so a mismatch between what the request actually sends and what the model's chat template assumes by default could produce exactly this symptom (thinking content leaking into visible output, or vice versa). Needs the actual raw response content to diagnose further — see if Rob can share the log/output once available.

- **MiniMax-M3-AWQ-INT4 (`logs/mmax3q/20260903_152700/`): silent, undetected 5-minute startup hang, EngineCore stuck in `torch.cuda._lazy_init()` — unrelated to the GLM-5.2 shm_broadcast hang saga below, root cause found and a fix drafted, 2026-09-03.** Model loaded fine (weights at 15:20:46, CUDA graphs captured 15:21:23) but the job never reached `/health`; `report_memory()`'s periodic snapshot showed GPU util pinned at 0% and `EngineCore` RSS slowly creeping (899M→2188M) for 5 straight minutes before Rob aborted it. Two `py-spy` captures ~4 minutes apart, both identical, "active+gil" (single-threaded, genuinely blocked, not making progress): `EngineCore`'s main thread frozen inside `torch.cuda._lazy_init()`, reached via `Scheduler.__init__()` → `supports_multimodal_inputs()` → resolving the MiniMax-M3 model class → importing `vllm/models/minimax_m3/nvidia/model.py` → `fused_allreduce_gemma_rms_norm.py` → `vllm/compilation/passes/fusion/allreduce_rms_fusion.py`, whose module-level `import flashinfer.comm` triggers flashinfer's own eager `torch.cuda.get_device_capability()` probe (`flashinfer/jit/env.py`) — in `EngineCore`'s own process, which had never previously touched CUDA (all 4 Worker processes were confirmed genuinely idle and healthy, correctly waiting on their shm-broadcast queue — this is not the GLM-5.2 cross-rank NCCL desync pattern below, no GPU/network involvement at all). Nothing in ivllm's instrumentation catches this class of hang: it happens inside `EngineCore.__init__`, before vLLM's HTTP server or `/health` endpoint exist, so the startup loop just logs "waiting for vLLM /health" indefinitely — there's no overall startup wall-clock timeout independent of health-check success, and the idle-timeout/heartbeat mechanism only arms once status reaches `running`.

  **Why `IVLLM_DISABLE_FLASHINFER=1` (set in this job's yaml) didn't prevent it**: that flag (`disable-flashinfer-env.v0.25.1.v1.patch`, see `design/prototype/patch/README.md`) is a runtime early-return inside `_can_use_flashinfer()` — it stops the fused kernel from being *used*, but does nothing about `import flashinfer` itself, which two vLLM modules do unconditionally at module level, guarded only by `try/except ImportError` (which doesn't help — flashinfer *is* installed, it just hangs instead of raising): `vllm/compilation/passes/fusion/allreduce_rms_fusion.py` and `vllm/distributed/device_communicators/flashinfer_all_reduce.py`. Both get pulled in transitively the moment any code imports a model file that imports `fused_allreduce_gemma_rms_norm.py` (MiniMax-M3, GLM-5.2), independent of whether the fused kernel is ever called.

  **Fix v1 tested live — cleared the hang, but exposed a design flaw.** `disable-flashinfer-import.v0.26.0.v1.patch` (same `IVLLM_DISABLE_FLASHINFER` guard around both import sites) was applied to the real venv: `logs/mmax3q/20260903_161157/` confirms the hang is gone, ran to serving. But the API returned syntactically-valid, completely incoherent output (degenerate repeated-token loops — `Modifier Modifier Modifier...`, `t t t t...`, `ayayaya...`). No crash, no NaN/error, ~35-45s of sustained 100% GPU util per request — a real generation, just numerically wrong. Rob then tested removing `IVLLM_DISABLE_FLASHINFER` (since this job is single-node, TP=4 — `knowledge-base.md` scopes that flag to *multinode* only) to see if the fallback path was the culprit: **the `EngineCore` import hang came straight back**, confirmed. This is exactly what v1's design predicts — v1 ties "does `EngineCore` even attempt the import" to the *same* flag that governs "do Workers use the fused kernel," so there was no way to get both "no hang" and "Workers use the real flashinfer path" at once.

  **Root design fix, v2, drafted 2026-09-03**: Rob's framing — *"if we enable flashinfer we need to be explicit about which path it should take"* — decouple the two questions. `disable-flashinfer-import.v0.26.0.v2.patch` adds a second, independent guard: detect `EngineCore`'s own process via `setproctitle.getproctitle()` (vLLM itself sets this — `"VLLM::EngineCore"` / `"VLLM::Worker_TP0_EP0"` — confirmed by the exact strings `py-spy` captured while diagnosing the original hang, so this is an observed signal, not a guess) and **always** skip the import there, unconditionally — `EngineCore` never calls anything else in either patched file (every use of `flashinfer_comm` in `allreduce_rms_fusion.py` is inside a function body or behind `if flashinfer_comm is not None:`, so this is provably dead weight in that process). Workers keep importing flashinfer normally, gated only by `IVLLM_DISABLE_FLASHINFER` as originally intended. Verified to apply cleanly (`git apply --check`) against `vendor/vllm-0.26.0`, and round-tripped (apply for real → confirm → `git apply -R` → confirm pristine again) since `vendor/` is gitignored and `git status` can't verify it directly. **Not yet applied to the real venv or tested live.**

  **Recommended next test**: apply `v2` in place of `v1`, and since this job is single-node, **remove `IVLLM_DISABLE_FLASHINFER` from the yaml entirely** (or set `VLLM_FLASHINFER_ALLREDUCE_BACKEND=trtllm` explicitly, matching `knowledge-base.md`'s documented node-local-safe backend, rather than relying on auto-detection) — this should both keep `EngineCore` from hanging (v2's unconditional guard) and let Workers use the real fused kernel (no longer forced onto the fallback), which would confirm or rule out the fallback-path-correctness hypothesis in one test. See `design/prototype/patch/README.md` for the full writeup, including a still-open note: the currently-deployed `disable-flashinfer-env` fix is confirmed live on the cluster (its `info_once` log line appears in every `mmax3q` run) but has no tracked `v0.26.0` diff in this repo, only a `v0.25.1` one — worth reconciling.

  **Second, independent lead on the garbage output, found 2026-09-03**: `examples/minimax-m3-int4.yaml` documents this checkpoint as requiring the `toncao/vllm@minimax-m3-compressed-tensors` fork branch, previously treated as "clone a separate fork/venv." Investigated whether it's patchable against `vendor/vllm-0.26.0` instead — see `design/prototype/patch/README.md`'s `minimax-m3-indexer-unfuse.v0.26.0.v1.patch` entry for the full writeup. Short version: most of that fork's changes are already independently present in `v0.26.0` upstream (pure kwarg-ordering no-ops); the one real, still-needed piece is un-fusing the sparse lightning-indexer's `index_q`/`index_k` projections out of `MinimaxM3QKVParallelLinearWithIndexer`'s single packed GEMM in `vllm/models/minimax_m3/{nvidia,amd}/model.py`, because that fused GEMM can't correctly represent a checkpoint that quantizes q/k/v to INT4 while leaving the indexer in bf16 — exactly `cyankiwi/MiniMax-M3-AWQ-INT4`'s situation. This would corrupt the indexer's output, feeding wrong block-selection scores into the model's custom sparse-attention Triton kernels (`_topk_index_kernel`, `_index_block_score_kernel`, both observed being freshly JIT-compiled during this exact model's serving) — a plausible, independent explanation for coherent-API/garbage-content generation, orthogonal to the flashinfer-fallback hypothesis above (different files, different mechanism — worth testing both, ideally not at the same time, to know which one actually fixed it). Patch drafted and verified to apply cleanly against `vendor/vllm-0.26.0`; not yet applied to the real venv or tested live.

- **`NCCL_GDRCOPY_ENABLE=1`/`FI_HMEM_CUDA_USE_GDRCOPY=1` are enabled as the shared default for every job in `vllm-env.sh` (since commit `244246c`, 2026-09-03) — genuinely unresolved, not a simple regression as first suspected, needs an empirical network test to settle.** This directly contradicts this same document's own earlier closed verdict below (CXI defaults to DMABUF, `NCCL_GDRCOPY_ENABLE` absent from the vendored NCCL reference, and a real CXI-specific libfabric double-free race, [ofiwg/libfabric#10041](https://github.com/ofiwg/libfabric/issues/10041), with no documented workaround) — but per Rob (2026-09-03), that earlier verdict was itself never settled by an actual measurement, only by reading reference docs, and there's real conflicting evidence it didn't have: Isambard's own container definition ([UKGovernmentBEIS/isambard_containers](https://raw.githubusercontent.com/UKGovernmentBEIS/isambard_containers/refs/heads/main/definitions/vllm/vllm.def)) changed to enabling it, and HPE's own [`shs-nccl-env`](https://github.com/HewlettPackard/shs-nccl-env) (the vendor's own Slingshot NCCL tuning tool) validates using GDRCopy. **Decision**: this can only be resolved with a real network benchmark (GDRCopy on vs. off, same collective workload, compare) — see the new "standalone multinode network benchmarking harness" priority below, an evolution of `design/prototype/slingshot-tp-reprex.sh`. Until that exists and is run, this stays enabled, unverified either way, per "if it isn't broken, don't fix it."

- **Batch of confirmed bugs/regressions found during the 2026-09-03 documentation-drift review (fan-out across `common-env.sh`, `vllm-env.sh`, `ray-setup.sh`, `slurm-vllm-setup.sh`, `ivllm-serve.sh`, `ivllm-bench.sh`, `ivllm-cancel.sh`, `ivllm-get-model.sh`, `ivllm-setup.sh`, and the full test suite) — not fixed this session per explicit instruction (no production code changes):**
  - **`capture_job_diagnostics()` no longer makes copied diagnostic files group-writable — resolved, 2026-09-03, not a regression.** An explicit `chmod g+rwXs "$diag_dir" "$diag_dir"/*` existed (added in commit `1106c4c`, "fix permissions issues") and was dropped in commit `6487602` ("multinode ray support"), but **Rob**: "That requirement dated from the time that the slurm scripts were copied into the job directory from the local client. Its unnecessary now because the job directory is only ever populated with files that are generated by ivllm on the HPC side, and the script files are copied into the users ~/.local/bin." Since every file under `$job_dir` is now created by an ivllm process running under this project's own `umask 0002` + setgid parent dirs, it's already group-writable before `cp` ever sees it — GNU `cp`'s default mode rule (`dest_mode = source_mode & ~umask`, can only shrink source permissions, never grant bits the source lacked) no longer has a "restrictive source" case to bite on in practice. The chmod was defense-in-depth against a scenario (locally-authored files landing in the job dir) that no longer exists in this architecture. `test-diagnostics.sh`'s `capture_job_diagnostics_forces_group_writable_even_from_restrictive_source` (which manufactured that scenario artificially via `chmod 644`) has been removed as obsolete rather than fixed.
  - **`resolve_job_log()` touching the log file if missing — resolved, 2026-09-03, intentional behaviour change, not a regression.** Does `if [[ ! -f $log ]]; then touch "$log"; fi` (added in commit `d4d0700`, "refactor debugging"). **Rob**: "This is an intentional change of behaviour. At one point I used log presence as indicator but this was fragile and changed it (about the same time the node suffix became mandatory even for single node jobs). `resolve_job_log` should create the file if it doesn't exist." Its own doc comment ("Creates it if it does not exist") already matches this — the stale part was purely the test. **Consequence worth remembering**: since `resolve_job_log()` can create the file itself rather than relying solely on SLURM's `--output` redirection having done so first, every ivllm process that writes to this log must *append*, never overwrite/truncate, or it risks racing/clobbering another writer's content. `test-lockfile.sh`'s `update_initialise_does_not_create_log` (which asserted the old, now-superseded behaviour) has been rewritten as `resolve_job_log_creates_file_if_missing` to assert the current contract instead.
  - **Bare, unguarded `$USER` in `report_memory()` (`utils.sh:1883`) and `report_processes()` (`utils.sh:1958`)** — both do `ps -u "$USER" ...` with no fallback, inconsistent with the rest of the codebase's `$(whoami)` convention (e.g. `ivllm-cancel.sh`, `is_cancellable()`). Only surfaces as a hard failure under `set -u` (the sandboxed test harness), not in production SLURM jobs (no `set -u` there) — but real fragility, and the exact reason `test-report-memory.sh` throws `USER: unbound variable`.
  - **`report_processes()` also references `$debug_level`, never set in its own scope** (leftover from when this logic lived inside a monolithic `report_memory()`, which did have a local `debug_level`, before the "refactor debugging" commit split it out) — silently prints an empty value in one log line in production (no `set -u` there to catch it). Same root cause as, and independently confirmed alongside, the "Four small bugs" entry below.
  - **`run_vllm_warmup()` gained an unconditional `sleep 10` immediately after its very first ping request** (`utils.sh` ~2111-2122, commit `244246c`) — every job now takes at least 10 extra seconds to reach `running`, unconditionally, regardless of how fast the model actually warms up. May be deliberate (settling time) or a debugging leftover never removed — worth confirming intent, since it's now a permanent latency cost on every job. Also broke three `test-monitor-head.sh` cases whose polling windows were written before this delay existed.
  - **`is_startable()` (`utils.sh:928-952`) is dead code** — implements the identical pending/initialising/running/warmup/cancel status-guard logic (down to matching `"[serve] ERROR: ..."` message text) as `ivllm-serve.sh`'s own inline `if is_status ...` block (lines 125-144), but nothing in the codebase actually calls it (`grep -rn "is_startable\b" src/engine/` finds only its own definition). Looks like an incomplete refactor — the two copies already drifted once (the `warmup` case was added directly to `ivllm-serve.sh`'s inline block, not by updating `is_startable()`, even though `is_startable()` has always had one). **Rob, 2026-09-03: "Needs refactoring."** Confirmed real, queued — see `design/priorities.md`.
  - **`src/engine/ivllm-bench.sh` — resolved, 2026-09-03.** Was promoted from `design/prototype/ivllm-bench.sh` to production status (copied byte-for-byte in commit `df67993`, with the `design/prototype/` original later deleted in `7ce59a7`) with its header still literally claiming "PROTOTYPE, not production... must be completely rewritten," despite already being called directly in production by `IsambardBareMetalBackend.ts` (`requestBenchmark`/`getBenchmarkStatus`/`fetchBenchmarkResults`) and wired into the real CLI (`bench submit|status|results`). **Rob**: "Have updated the header. It's been reviewed. It should work, but not yet end to end tested. It is experimental lifecycle." Header now reads `lifecycle: experimental` + an explicit "has not been end to end tested" note instead of the stale prototype disclaimer. E2E testing is the remaining gap — see `design/complete/bench-implementation-plan.md`.
  - **`tests/bash/sandboxed/test-ivllm-bench.sh` is completely broken as a direct consequence** — all 5 cases `source /work/prototype/ivllm-bench.sh`, bound by the sandbox harness to `design/prototype/ivllm-bench.sh`, which no longer exists (deleted in `7ce59a7`, after the promotion above). Verified by running the suite: `source: ... No such file or directory` on every case, all 5 fail.
  - **Stale, actively misleading comments sitting directly above correct code**: `ivllm-get-model.sh:103` and `ivllm-setup.sh:72` both still read `# --partition=interactive \ seems unnecessary?` immediately above a line that *does* pass `--partition=interactive` — the comment predates commit `d4d0700` re-adding the flag (after an earlier commit, `3db4793`, had removed it and left the comment behind) and was never cleaned up. The flag itself is correct per this doc's own `Hidden=YES` finding below (`--reservation=interactive` alone isn't sufficient for job visibility) — only the leftover comment needs removing.
  - **A real, now-already-fixed bug, worth recording for the history**: `ivllm-cancel.sh`'s force-cancel path used to compare an undefined `$type` (a variable only ever `local` inside a *different* function, `request_cancel()` in `utils.sh`) against `"cancel"` — always false, meaning `ivllm-cancel.sh -f` (force cancel, no abort) always silently took the *abort* branch (exit 254, `failed`, unconditional `capture_job_diagnostics()`) regardless of whether `-a` was passed. Fixed in the current `HEAD` by comparing `$IVLLM_ABORT` instead. No action needed — noted here only so the historical symptom (every force-cancel looking like an abort) isn't mysterious if it shows up in old logs.

- **Four small code smells found during a 2026-09-03 documentation-drift review of `src/engine/lib/utils.sh`** — two were real and are now fixed/tracked, two turned out to be intentional (not bugs):
  1. **RESOLVED, not a bug — `node_hang_detector()`'s double `sleep "$IVLLM_CHECK_INTERVAL_SECS"`.** Rob: "this was an intentional hack to work around the issue that it was occasionally firing during setup." A `# TODO: resolve this workaround for accidental triggering by checking...` comment has been added at the call site so the intent is now documented in-code, not just here.
  2. **FIXED, 2026-09-03 — `report_processes()`'s undeclared `$debug_level` reference** (was printing an empty value in its "py-spy not installed" message). Message simplified to no longer reference it at all. The related `node_hang_detector()`/`$node` implicit-scope-coupling observation stands as a minor style note, not treated as a bug needing action.
  3. **RESOLVED, not a bug — `semver_sort`/`rev_semver_sort` naming.** Rob: "Comments make it clear. Depends on how you interpret the natural order of versions which in my case is most recent first." I.e. "sort" without qualifier means Rob's own default ordering (descending, most-recent-first); `rev_` is the reverse of *that*, not a generic ascending/descending label — internally consistent once you know the convention, not backwards.
  4. **`report_torch()`'s own header comment already states the flight-recorder mechanism it drives "does not currently result in anything informative... the torch output is nonsense"** — this matches, independently, everything this document's GLM-5.2 entry found the hard way about the always-empty `torch-nccl-node-*-rank-*` dumps across every run in that investigation. Still open, no action taken.

- **CUDA device-state capture on a live vLLM worker has a genuine catch-22: no combination of the two available mechanisms gets you a valid device-side (kernel/warp/register) snapshot from a process you still intend to keep running.** Found during the GLM-5.2 coredump-tooling work, 2026-09-01/02.

  - **`report_cuda()`'s live `cuda-gdb -p <pid>` attach never returns device-side info, on this platform, full stop.** Checked every `cuda-gdb-node*.log` produced across the entire GLM-5.2 investigation (dozens of runs, both nodes) for any sign `-ex "cuda thread apply all backtrace"` ever returned *anything* — a kernel listing, block/thread indices, even cuda-gdb's own "No CUDA kernels" fallback message. Zero matches, ever. Every invocation is independently confirmed `Killed` by its `timeout -s 9 10s` wrapper rather than exiting cleanly via its own `-ex quit`, and the CUDA driver's own `"CUDA coredump is not supported with late attach. Disabling coredump."` message fires synchronously, adjacent in the log, to `report_cuda`'s own attach — i.e. attaching a live debugger to an already-running CUDA context is itself what triggers that message and disables device debugging for the process, on this driver/platform combination. The plain host-side `thread apply all backtrace` (everything this investigation actually used) does work and return promptly; only the device-side half is dead.
  - **The alternative — a user-triggered `CUDA_COREDUMP_PIPE`/`CUDA_COREDUMP_FILE` dump — only produces a valid, readable coredump when the `skip_abort` flag is *not* set.** With `skip_abort` (the flag combination needed to avoid killing the process, so capture could in principle be used repeatedly for ongoing monitoring), dumps come back truncated: `readelf -h` shows a "start of section headers" offset tens of gigabytes past the actual (~2GB) file size — confirmed on multiple independent dumps from the same run, all with the identical shape. This matches vLLM's own documented warning (`2025-08-11-cuda-debugging.md`) that `skip_abort` "has a documented bug that may cause illegal memory access errors on the GPU to be ignored," and is consistent with `skip_abort` letting the GPU context keep mutating state while the (large, non-memory-skipped) dump writer is still working, racing itself. Dropping `skip_abort` produces a valid, readable dump — but its side effect is that it **aborts the parent process** once the dump completes, so it can only ever be a one-shot terminal capture, never a repeatable observability mechanism you can fire against a still-being-monitored server.

  **Net effect**: today, getting a real device-side snapshot of a hung vLLM worker requires deliberately sacrificing that worker (`CUDA_COREDUMP_GENERATION_FLAGS` without `skip_abort`) — there is no way to capture one non-destructively for a process you want to keep serving from afterward. Not yet investigated: whether any other flag (rather than dropping `skip_abort` entirely) avoids the truncation while still avoiding the abort, or whether this is a hard tradeoff built into the mechanism on this CUDA/driver version.

- **vLLM's `--config <file>.yaml` loader silently drops boolean `false` values, defeating any flag whose real default isn't `False`** (upstream vLLM bug, `vllm/utils/argparse_utils.py`, confirmed against v0.25.1 via `vendor/vllm`). The yaml→argv conversion does:
  ```python
  if isinstance(value, bool):
      if value:
          processed_args.append("--" + key)
      # else: nothing appended — False is silently dropped
  ```
  This assumes every boolean flag's off-state is "flag absent," which is only true for flags whose CLI default is already `False`. For tri-state fields (`bool | None = None`, resolved later by other defaulting logic) omitting the flag does **not** mean off — it means "let some other default win," and that default is frequently `True`.

  **Confirmed affected, found during the MiniMax-M3 investigation (2026-08-07):**
  1. `enable-flashinfer-autotune: false` — `KernelConfig.enable_flashinfer_autotune` defaults to `None`, later filled in by the active `--optimization-level` preset (default **O2** → `True`; only `O0`'s preset defaults it to `False`). Every `vllm.yaml` in this repo that set `enable-flashinfer-autotune: false` had it silently ignored — flashinfer's autotune warmup dummy-run kept running every time, and was the actual root cause of a reproducible startup OOM chased across many separate MiniMax-M3 debugging sessions before this was traced. Since ivllm passes yaml straight through via `vllm serve --config vllm.yaml.clean.yaml`, every job in this repo relying on a bare `false` for this flag has been affected without any error or warning.
  2. `disable-hybrid-kv-cache-manager: false` — `SchedulerConfig.disable_hybrid_kv_cache_manager` has the identical `bool | None = None` shape, resolved later by several auto-disable heuristics in `VllmConfig` (platform support, chunked-local-attention+eagle, KV connector HMA support). Found in a DeepSeek config; didn't change observed behaviour in that case only because the heuristics happened to resolve to the same value anyway — same underlying defect.

  **Workaround** (proven working — already how `compilation-config`'s `fuse_allreduce_rms: false` correctly takes effect): wrap the field in its corresponding JSON-blob CLI arg instead of a bare top-level yaml key, e.g. `kernel-config: '{"enable_flashinfer_autotune": false}'` instead of `enable-flashinfer-autotune: false`. Dict-valued yaml keys are JSON-serialized as-is by the same loader and don't hit this code path.
  `no-enable-flashinfer_autotune: true` pattern is also an alternative.

  **Status**: reported upstream to `vllm-project/vllm`. Any `vllm.yaml` in `examples/` using a bare `key: false` for a flag whose default isn't already `False` should be audited and switched to the `*-config` JSON-blob form. This is fixed upstream but not sure what release it will be in.

- **SLURM (platform-level, not an ivllm bug): any job requesting more than 2 nodes in the "interactive" partition/reservation reliably gets step creation stuck on the 3rd node onward.** First seen via real `glm52`/`glm52q` jobs (job step creation for nodes 3/4 stuck indefinitely on `srun: Job step creation temporarily disabled, retrying (Requested nodes are busy)`, while nodes 1-2 come up fine every time), 2026-08-08 onward. Confirmed not specific to 4-node jobs — applies to any job over 2 nodes; "4-node" is just the case most tested since that's this partition's documented max.

  **Ruled out, in order**: wall-time/node-hours misunderstanding (`--time` is plain wall-clock, not multiplied by node count); `--exclusive` not being honoured (job-level allocation genuinely succeeds on all requested nodes every time — confirmed via `AllocMem`/`RealMemory` matching on the stuck nodes too, so the *allocation* is fine, only *step creation* on top of it fails); a client-side launch race in ivllm's own worker-loop (tested a `sleep 2` stagger between per-node `srun --overlap` calls — no difference); one specific broken physical machine (the identical positional pattern — always the 3rd/4th node, never the 1st/2nd — recurred across multiple, entirely different physical node sets, ruling out hardware); the QOS "2 total jobs" cap an Isambard admin (ticket #372791, "Anna") initially suspected (re-tested with a confirmed single job in the reservation via `squeue --all`, issue persisted); and, most recently, the `--cpus-per-task=72` CPU-allocation bug found during the GLM-5.2-INT4 investigation (see the `Mitigated` entry below — fixing it does not change this issue at all, confirmed by direct retest).

  **Real, separate finding along the way, not the root cause but worth keeping**: Isambard's "interactive" partition/reservation is configured `Hidden=YES`, so a plain `squeue --me` silently omits jobs running there entirely — `squeue --all` (or `--partition=interactive --reservation=interactive`) is required to see them at all. This cost real debugging time before being identified; `design/prototype/diagnose-stuck-nodes.sh` (built to automate diagnosis of this issue) accounts for it.

  **Decisive result: reproduced with zero vLLM/Ray-application/ivllm-dispatch-script code involved.** `design/prototype/ray-4node-smoketest.sh` — a from-scratch script requesting the identical resource shape ivllm's real jobs use (`--gpus-per-node=4 --cpus-per-gpu=64 --mem=0 --exclusive`, one `srun --overlap` per node) but running nothing but a plain `ray start` and a trivial Python payload — hit the exact same failure (`logs`/output pasted into conversation, 2026-08-11, job `5989979`): `squeue -s` after all 4 launches shows only steps `.0` (head) and `.1` (worker 1) ever created; workers 2 and 3 never get a step at all, at any point, before or after a 30s settle window. `ray.nodes()` confirms only 2 of 4 nodes ever joined the cluster. This is as close to definitive as evidence gets without SLURM-controller-side access: the failure is a property of this cluster's SLURM scheduler when a 3rd/4th `srun --overlap` step within one job requests GRES on this partition/reservation — not anything in vLLM, Ray, or any of ivllm's own scripts.

  **Status**: raised with Isambard support (ticket #372791) with the smoke-test evidence attached. This is outside ivllm's ability to fix — resolution depends on Isambard admin action (checking `slurmd`/GRES bookkeeping behaviour on this partition, or a scheduler-side limitation on concurrent overlapping GRES-requesting steps per job). No further ivllm-side code changes are expected to help; only additional diagnostics if requested by support.

- **JIT cache growing without converging — RESOLVED, 2026-09-03.** `resolve_job_jit_cache()`'s "trust each subsystem's own content-addressed caching, don't key by node" design turned out to be sound in principle; two separate node-identity leaks were breaking it in practice, both now fixed (see below). Originally noticed during the GLM-5.2-INT4 investigation (`logs/glm52q/`): the shared JIT cache tarball reached ~7GB uncompressed (~900MB compressed) before being manually deleted, 2026-08-11 — described by the person debugging it as "quite unusual" growth, not the mild/expected kind.

  **Design context**: `resolve_job_jit_cache()`'s hash (model/TP/DP/PP/EP/vLLM-version only, deliberately excluding physical node/hostname identity — see the `Mitigated` entry below for the full reasoning) assumed this was safe because Triton/Inductor/DeepGEMM/flashinfer all do their own internal content-addressed caching, so different nodes landing in the same shared bucket would just additively coexist without correctness risk, converging toward a stable size once every genuinely distinct kernel variant had been seen. Sustained, non-converging growth run after run — for what should mostly be the same model/config — is inconsistent with that assumption holding.

  **Leading hypothesis**: `torch.compile`'s own on-disk cache path may bake in something machine-specific (e.g. exact GPU device identifier, not just architecture/compute-capability) rather than being purely content-keyed. With this reservation's ~1300-node pool, a long investigation session essentially never reuses the same physical hardware twice — so every run would add one node's worth of "new" entries for what might be identical kernel content, with no convergence ever possible. Directly observed (different investigation, `20260811_101945/`) that two different ranks got different `torch_compile_cache/<hash>/rank_N_0/` subdirectory names on two different nodes — consistent with, though not yet decisive proof of, this theory (the two data points were different ranks, not the same rank on two different nodes).

  **New correlation worth tracking**: `VLLM_USE_AOT_COMPILE=1` was absent from every one of the 21 `glm52q` runs before `logs/glm52q/20260811_154046/`, and present in every one of the 6 runs since (`154046`, `161429`, `164159`, `171124`, `172729`, `222935`) — exactly the window in which the growth was flagged as unusual. AOT compilation serializes a complete compiled artifact per rank (`saved AOT compiled function to .../torch_aot_compile/<hash>/rank_0_0/model`, already visible in earlier logs), plausibly much larger per-entry than incremental Inductor FX-graph-cache metadata — so if the per-node non-convergence theory above is right, `VLLM_USE_AOT_COMPILE` could be substantially amplifying the size of each new node's "wasted" contribution rather than causing the non-convergence itself. Not yet isolated from the node-identity variable.

  **Not yet done**: the decisive check would be comparing the `torch_compile_cache/<hash>/rank_0_0/` hash for the *same rank* across two runs confirmed to have landed on two *different* physical nodes, same model/config/AOT-compile setting otherwise — same hash both times rules out node-identity baking; different hash confirms it directly.

  **Practical mitigation applied, not a fix**: cache manually deleted 2026-08-11. Given how many one-off configs this investigation cycles through (and potentially now how many distinct physical nodes too), the cache is plausibly providing little real reuse benefit at this stage while adding real save/restore overhead every run — periodic manual deletion is reasonable for the duration of active investigation. Revisiting the cache-key granularity itself (e.g. whether `torch.compile`'s cache specifically needs node-scoping that the rest of the JIT cache doesn't) is lower priority than the main GLM-5.2 hang above and not yet started.

  **Root cause confirmed, 2026-09-03: the "leading hypothesis" above was right, and the specific mechanism is now known.** `$VLLM_CACHE_ROOT/torch_compile_cache` and its `torch_aot_compile` subdirectory each write a `cache-key-factors.json` per run-specific subdirectory, and that key is computed from (among other things) `VLLM_XLA_CACHE_PATH` — which `set_jit_caches()` sets to a **node-local** path (e.g. `/local/user/<uid>/<hostname>/<job>/xla`). Since that path embeds the hostname, every physical node this job lands on produces a different `torch_compile_cache` hash for otherwise-identical compiled artifacts, exactly the non-converging-growth pattern described above — this wasn't a `torch.compile`-internal machine fingerprint as originally suspected, it was ivllm's own node-local `XLA_CACHE_PATH` value leaking into a cache key that has nothing to do with XLA (GLM-5.2/this project's models don't use XLA at all — the variable being read into the hash appears to be a generic catch-all "path-like env vars" fingerprint on vLLM's side, not something specifically about XLA usage).

  **First fix applied**: removed `VLLM_XLA_CACHE_PATH` from `set_jit_caches()` (`src/engine/lib/utils.sh`) entirely, since nothing in this project's model set uses it and its only observed effect was poisoning this cache key with node identity.

  **Second, deeper cause found and fixed, 2026-09-03**: removing `VLLM_XLA_CACHE_PATH` stopped the cache *key* from varying by node, but a separate problem remained one level down — vLLM's own `InductorStandaloneAdaptor.initialize_cache()` (`vendor/vllm-0.26.0/vllm/compilation/compiler_interface.py:463-480`) bakes the node-local scratch path itself into the compiled artifacts it writes under `torch_compile_cache/<hash>/rank_i_j/inductor_cache` (it explicitly tracks `compiled_fn.__code__.co_filename` and expects it to start with the cache dir's own absolute path). Since `resolve_localdir()` built that path as `/local/user/<uid>/$(hostname -s)/<job>` (`utils.sh:111`, now superseded), every node produced artifacts baked with a different, node-specific absolute path — non-portable even once the cache *key* itself converged, breaking restores of a cache tarball saved on one node onto a different node (or even the same node on a later job with a different job name). **Fix**: dropped the hostname component from `resolve_localdir()` entirely — `node_local="$LOCALDIR/$job"` instead of `"$LOCALDIR/$(hostname -s)/$job"` (`utils.sh:111-117`). Safe because `/local` is genuinely a per-node RAM-tmpfs mount (never shared across nodes), so the hostname was never providing real isolation — `$LOCALDIR` (`/local/user/<uid>`) already scopes by user, and `$job` already scopes by job; two nodes never see each other's copy of this path regardless of what string it contains. This makes every node produce the *identical* absolute local-scratch path for a given job, so paths baked into `torch_compile_cache` (and anything else that does the same thing) stay valid no matter which node restores the cache. **Confirmed working by Rob, 2026-09-03: verified across 2 runs on qwen38.**

- **Remaining answers from Rob's 2026-09-03 review of `design/questions-for-rob.md`**, recorded here for permanence (that file's answers are the working notes; this is where they land):
  - **`slurm-vllm-setup.sh` dropping `#SBATCH --reservation=interactive` was deliberate, architectural.** Rob: "All partition decisions made by scripts that call slurm scripts (and not the scripts themselves), so that it can be affected by CLI flags. Setup as it happens always uses interactive partition when called through CLI." I.e. the SLURM batch scripts under `lib/` are meant to be partition-agnostic; the login-node CLI wrappers (`ivllm-setup.sh` etc.) own the partition decision so it stays a single, overridable point of control rather than being hardcoded in multiple places.
  - **`IVLLM_PARTITION`'s default flip-flopping across recent commits is not settled drift — it reflects a genuinely moving target.** Rob: "Keeps changing on isambard. Current state is both flags are needed" (`--partition=interactive --reservation=interactive`, currently correct per the `Hidden=YES` finding above). Worth treating this value as something to re-verify if Isambard's own partition/reservation behavior changes again, not a fact to assume stays true indefinitely.
  - **`ray-setup.sh`'s commented-out `#--include-dashboard=false` — tried once, inconclusive.** Rob: "Tried to disable it but Ray crashed (not sure whether that was incidental) at some point I would like to retest but low priority." Left commented out for now; genuine open retest, not urgent.
  - **`request_cancel()` calling `tidy_up` from the login node (for a `pending` job with no compute-side monitor yet) is fine — `tidy_up` was never actually compute-node-only.** Rob: "Nothing in tidy_up's code says it must be run on compute node — only node-specific action is clearing the localdir (which is probably not in the right place anyway — and probably only works for head node of a cluster)." **New finding worth its own follow-up**: `clear_localdir()`'s placement/multi-node correctness is now flagged by Rob himself as suspect — worth checking whether it's actually cleaning up worker nodes' local scratch too, or silently only ever touching the head node's, next time anyone is in that code.
  - **`VLLM_NCCL_SO_PATH`'s dangling comment in `common-env.sh` is a real, intentional placeholder, not abandoned scaffolding.** Rob: "placeholder for investigation. The mechanism exists in the documentation with the comment: Path to the NCCL library file. It is needed because nccl>=2.19 brought by PyTorch contains a bug: https://github.com/NVIDIA/nccl/issues/1234 — maybe worth exploring in the VLLM codebase." The issue number is real (from vLLM's own docs), not a typo/placeholder.
  - **`LD_PRELOAD=".../libcudadebugger.so.1"` (`common-env.sh`) is required for the cuda-gdb/coredump mechanism to work at all.** Rob: "not sure how or whether this got validated. Not broken don't fix." Left as-is.
  - **`FI_CXI_DEFAULT_RX_SIZE` — removed.** Replaced with `FI_CXI_RDZV_EAGER_SIZE=0` (an HPE Slingshot vendor default, per [`shs-nccl-env`](https://github.com/HewlettPackard/shs-nccl-env)), which Rob notes "has no particular effect" so far — same conclusion as the GDRCopy question above: needs the network benchmark harness (see priorities) to actually measure rather than guess.
  - **The `common-env.sh` TODO pasting `"Could not find: libnccl-env.so"`/`"libnccl-profiler.so"` log lines is mostly noise, kept as a breadcrumb.** Rob: "I think these are noise. NCCL profiler would be potentially useful, libnccl-env.so could be using HP[E, presumably a vendor-specific optional plugin]." Not acted on further.
  - **`backend-contract.md` §2.5 (`requestCancel`) should be updated to include the `abort` parameter, and the lockfile schema should include `warmup`.** Rob: "Yes abort and warmup." Both applied.
  - **`design/complete/bench-implementation-plan.md`'s "Draft, unimplemented" status is stale — the prototype→production move already happened.** Rob: "E2E testing left" is the only remaining real gap, matching the `ivllm-bench.sh` entry above.
  - **Test suite: fix the stale tests, leave the two tests catching real regressions failing — done, 2026-09-03.** Rob: "Fix stale tests. Leave real regressions failing." Fixed 5 files: `test-wait-report.sh` renamed to `test-monitor-node.sh` and rewritten against `monitor_node()` (Rob confirmed the same exit-code-propagation contract still holds); `test-ivllm-bench.sh`'s source path updated from the deleted `design/prototype/ivllm-bench.sh` to `src/engine/ivllm-bench.sh`; `test-exit-trap.sh`'s stale `"SLURM timeout"` string assertion updated to `"SLURM job cancelled"`; `test-report-memory.sh` rewritten to test `report_memory()`/`report_gpu()`/`report_processes()` separately, matching the split; `test-monitor-head.sh`'s idle-timeout test rewritten to backdate the heartbeat file *after* starting `monitor_head` (it touches the heartbeat once, unconditionally, at its own startup — backdating first has no effect), and its two warmup-timing tests given a wider polling window + longer sandbox timeout to accommodate `run_vllm_warmup()`'s new unconditional `sleep 10`. All 5 fixed files pass; verified by running the full suite. `test-diagnostics.sh` and `test-lockfile.sh` were left failing at that point, per instruction — both have since turned out to be stale rather than real regressions (see the `capture_job_diagnostics()`/`resolve_job_log()` entries above) and were fixed on 2026-09-03: `test-diagnostics.sh` had its obsolete `capture_job_diagnostics_forces_group_writable_even_from_restrictive_source` case removed (the scenario it simulated can't occur in the current architecture); `test-lockfile.sh`'s `update_initialise_does_not_create_log` was rewritten as `resolve_job_log_creates_file_if_missing` to assert the current, intentional contract. Both files pass in full now. `test-login-handoff.sh`'s `login_force_cancel` still fails (non-root sandbox user, pre-existing environment limitation, not addressed either way — see its own entry below).

- **`login_force_cancel` test failure — needs confirmation, not yet root-caused.** `tests/bash/sandboxed/test-login-handoff.sh`'s `login_force_cancel` fails because `ivllm-cancel.sh -f` logs `"not possible to force cancel job owned by root"` and never calls `scancel`. The test's fixture lockfile sets `"user":"root"` but the sandbox actually runs as a non-root user, so this may just be an ownership check correctly doing its job against a stale test fixture (similar in flavor to the `2775` permissions-scheme staleness above) rather than a genuine bug — hasn't been dug into further.

## Mitigated (root cause unconfirmed)

- **Nemotron-3-Ultra-550B multinode (Ray, TP=8 across 2 nodes) hung reliably within minutes of every real run, 2026-08-04/05.** Every attempt (dozens, across the `multinode ray support` work) died the same way: `vLLM` served startup/warmup and a handful of real requests fine, then `EngineCore` logged `"No available shared memory broadcast block found in 60 seconds"` five times (~300s) before `TimeoutError: RPC call to sample_tokens timed out` → `EngineDeadError`.

  **Investigation path** (see conversation history for full detail; summarised here for anyone picking this back up):
  1. Initial log analysis found Triton kernels (`_zero_kv_blocks_kernel`, `_compute_slot_mapping_kernel`, `_causal_conv1d_fwd_kernel`, `eagle_*`) JIT-compiling during inference near the hang, matching several open vLLM GitHub issues (#45198, #43009) about warmup-coverage gaps under TP≥2. Testing with `speculative-config` disabled showed the hang moved *earlier* (into ivllm's own startup warmup) rather than disappearing — ruling out MTP/speculative-decoding as the cause, since the same core KV-cache/Mamba kernels are used with or without it.
  2. Added `IVLLM_RUNTIME_DEBUG` / `IVLLM_DEBUG_LEVEL` env-var-gated instrumentation to `wait_report()`/`report_memory()` (`src/engine/lib/utils.sh`) — extends periodic Cache/RAM/Top logging through the `running` phase (not just `initialising`), and at higher debug levels adds a per-GPU `nvidia-smi` snapshot and `py-spy dump --nonblocking` stack traces of every vLLM/Ray worker process, written to a persistent per-node file under the shared job dir (`$PROJECTDIR/engine/jobs/<job>/debug/pyspy-node<N>.log` — deliberately not `$localdir`, which is node-local tmpfs, invisible to other nodes, and wiped on shutdown).
  3. A real hung run captured with this instrumentation showed something different from the Triton-JIT theory: **all 8 worker ranks, both nodes, frozen at the identical frame** — `torch.cuda.graphs.CUDAGraph.replay()`, inside the MTP (`nemotron_h_mtp`) draft model's forward pass (`propose_draft_token_ids`) — no compilation involved, JIT had finished minutes earlier. The GPU-utilisation snapshot was the deciding evidence: 7 of 8 GPUs pegged at 100% (consistent with legitimately spinning inside a stuck NCCL collective) but **rank TP0/EP0 on the head node sat at 0%** the entire ~4-minute hang — everyone else waiting on it, it doing nothing.
  4. This matches [vLLM issue #46097](https://github.com/vllm-project/vllm/issues/46097) almost exactly: a multi-node TP CUDA-graph-replay NCCL collective deadlock, independently reported on a different model, diagnosed there as "the ranks are likely not in the same collective instance" — a tensor-parallel control-flow divergence, not a compile-timing issue. That thread's own discussion (read via the GitHub API, since `gh` isn't authenticated in this environment and the GitHub web UI doesn't render comments through `WebFetch`) found the divergence to be **NCCL-build-specific, not version-specific**: two builds of the identical version string (`2.28.9`) — a `pip` wheel vs. the OS vendor's `apt` package — behaved completely differently (deadlock vs. stable), identifiable only by SHA256/Build ID, not by version string, log banner, or `NCCL_VERSION` env var (all shown to be unreliable/stale in that thread).
  5. Built `design/prototype/nccl-probe.sh` (non-invasive, read-only) to check Isambard's actual situation: confirmed **torch/vLLM's default NCCL is the bundled `nvidia-nccl-cu12` pip wheel (2.28.9)**, not NVHPC 26.3's bundled copy (2.29.3) or anything from the `brics/nccl` module (which turned out to provide no `libnccl.so` at all — likely just the `aws-ofi-nccl` plugin). Confirmed via direct `ncclGetVersion()` ctypes calls (not `torch.cuda.nccl.version()`, which turned out to be **misleading in practice** — it kept reporting the old version even after the on-disk file was replaced and reloaded, implying it's a value baked into the torch wheel at torch's own build time, not a live query). Also confirmed `LD_PRELOAD`-ing an alternative NCCL does **not** override torch's own bundled copy — torch loads its own file by absolute path regardless, so both end up mapped and torch's own copy is what's actually used.
  6. Upgraded the venv's `nvidia-nccl-cu12` to `2.30.4` (available directly on PyPI — no source build needed) — the one build a third party in the #46097 thread reports as stable under sustained multi-node TP load for days. Re-ran `nccl-probe.sh`: confirmed via `ncclGetVersion()` that this is genuinely what loads now, and added a 2-node transport-selection smoke test (`NCCL_DEBUG=INFO` + a real `torch.distributed` `all_reduce` across both nodes) to check the upgrade doesn't break Isambard's Slingshot integration — confirmed `NET/OFI Selected Provider is cxi` (Cassini/libfabric genuinely in use, not a silent TCP fallback) and a mathematically correct `all_reduce` result, so the existing `aws-ofi-nccl 1.8.1-aws` plugin is compatible with the new core NCCL.

  **Fix applied**: `nvidia-nccl-cu12==2.30.4` is now pinned in `slurm-vllm-setup.sh`, unconditionally so it applies to pre-existing venvs too. **Location update, 2026-09-03**: originally placed right after the vLLM install step — this is exactly what let it be silently reverted for the GLM-5.2 0.26.0 venv (see the resolved GLM-5.2 entry below: `uv`'s dependency re-resolution on later installs in the same script re-pulled torch's own exactly-pinned 2.28.9). Now moved to the very end of the script, after every other install step (flashinfer-jit-cache, DeepGEMM, UCCL-EP, NIXL, humming-kernels, HPC-OPS) — confirmed deliberate, not coincidental drift.

  **Status**: `examples/nemotron-3-ultra-550B-A55B-FP8.yaml` promoted from `lifecycle: experimental` to `operational` (`logs/ntron550/20260805_134500_working/` — a real ~15-minute interactive session, clean user-cancel shutdown, no hang) on the strength of this evidence, but **the root cause is not fully proven** — no long soak test has been run, and the mechanism by which a specific NCCL build causes the TP rank divergence (rather than merely correlating with it) was never identified even in the upstream issue thread. If the hang recurs, re-enable `IVLLM_RUNTIME_DEBUG: 1` / `IVLLM_DEBUG_LEVEL: 2` (or `3`) in the job's `env:` block and check `debug/pyspy-node*.log` for the same frozen-rank signature before assuming this is unrelated.

- **GLM-5.2-AWQ-INT4 (TP=8×EP across 2×4-GPU nodes) hangs with the same "one rank stuck, rest spinning" signature as the Nemotron and MiniMax-M3 investigations above** (`logs/glm52q/`, 2026-08-10/11). Every attempt: real serving proceeds fine for a while, then `EngineCore` logs `"No available shared memory broadcast block found in 60 seconds"` repeatedly with no recovery, requiring a manual `ivllm cancel` (exit 254, "user abort"). Same class of hang as Nemotron's, on a third, unrelated model and a third quantization scheme (AWQ INT4). Extensively tuned; a real, independently-useful bug was found and fixed along the way (the CPU allocation issue below), but the core hang itself remains unresolved — **paused as of 2026-08-11**, this entry covers what's actually moved the needle vs what hasn't, so it doesn't need re-litigating from scratch.

  **Diagnosis (mechanism, consistent across every variant tried)**: `IVLLM_DEBUG_LEVEL: 3`'s pyspy traces plus periodic `report_memory()` GPU snapshots always show the same terminal shape: `EngineCore` blocked in `shm_broadcast`'s `dequeue`/`get_response`, waiting for a worker response that never arrives; N-1 of the 8 workers have already finished their step and are parked in a device-sync (`_bookkeeping_sync → _to_list` in `sample_tokens`, which forces a CUDA stream sync); exactly one worker is caught either (a) already back in its idle busy-loop having apparently exited a collective early with no exception logged (the async-scheduling variant, `step_with_batch_queue`), or (b) genuinely still inside a live collective, e.g. `ncclAllGather` in `compute_logits` (the no-async-scheduling variant, plain `step()`). Matches the TP-rank control-flow divergence class documented in vLLM issue #46097 (cited in the Nemotron entry above) and in [vllm-project/vllm#43420](https://github.com/vllm-project/vllm/issues/43420) (`RayExecutorV2`'s `shm_broadcast` assumes single-host shared memory, closed upstream as architectural/not-planned — a close mechanism match, though that issue's own downstream symptom, a 30-minute Gloo-TCP timeout, has never actually been observed here since jobs get manually aborted first).

  **Changes that measurably helped (keep these as the baseline going forward)**:
  - **`no-async-scheduling: true`** — changes which failure mechanism you hit, not just cosmetically: with async scheduling (vLLM's default) the hang reproduces fast via the `step_with_batch_queue` variant above; with it off, the hang shifts to the "genuinely-still-computing straggler" variant and survival time before hanging goes from near-immediate to many minutes of real serving. Confirmed in both directions — re-enabling async (`logs/glm52q/20260811_095817/`, done by accident while testing something else) reliably reproduced the fast hang again.
  - **`NCCL_NET_GDR_LEVEL: PHB`** (vs the default `SYS`) — cleanest isolated comparison available: `logs/glm52q/20260811_104623/` (PHB) sustained ~8m19s of real serving before hanging vs `logs/glm52q/20260811_092257/` (SYS, otherwise identical config) at ~2m49s — roughly 3x. Doesn't prevent the hang, delays it. Consistent with `PHB`'s stricter GPUDirect-RDMA eligibility check pushing whichever rank has flaky topology detection (see below) off a fast-but-unreliable path onto a slower, more conservative one.
  - **`--network=disable_rdzv_get`** (sbatch flag, added to pair with the pre-existing `FI_CXI_RDZV_PROTO=alt_read`) — per [HPE's Slingshot rendezvous-protocol docs](https://support.hpe.com/hpesc/public/docDisplay?docId=dp00005991en_us&page=user/rendezvous_protocol_configuration.html), `alt_read` requires this SLURM allocation flag to actually take effect; it had been missing from every prior run, meaning `alt_read` was silently half-applied the whole time. `logs/glm52q/20260811_104623/` (with this fix, on top of the PHB+no-async baseline) produced the single longest/most successful run yet — ~7.5 min of sustained serving, a dozen+ successful completions, survived a multi-minute mid-session idle gap that no prior config survived. The eventual hang, once it did occur, was byte-identical in signature to every other run. Real improvement, not a fix.

  **Changes tried that made no measurable difference**:
  - Worker-launch stagger (`sleep 2` between per-node `srun --overlap` calls) — ruled out a client-side launch race as the cause.
  - [uccl-project/uccl#956](https://github.com/uccl-project/uccl/issues/956)-inspired CXI tuning: `FI_CXI_RX_MATCH_MODE` (`software`→`hybrid`), `FI_CXI_DEFAULT_TX_SIZE` (`1024`→`2048`) — no change to the hang; if anything the run right after this change hung modestly *faster* (`logs/glm52q/20260810_215408/`, ~8m9s vs ~9m53s), though that's within plausible JIT-compile-time noise.
  - `FI_CXI_RDZV_THRESHOLD`/`RDZV_GET_MIN`/`RDZV_EAGER_SIZE=0` — turned out not to even fully apply: libfabric's own `cxip_env_init()` silently floors the threshold to 192 bytes regardless (visible once `FI_LOG_LEVEL=warn` was enabled).
  - Full revert of all `FI_CXI_*` overrides back to `vllm-env.sh`'s defaults (software match mode), keeping only `NCCL_NET_GDR_LEVEL: PHB` and `no-async-scheduling: true` — reported as making no difference to the underlying hang versus the PHB+no-async baseline.
  - **`numa-bind: true` — confirmed to never take effect, for two independent reasons found in sequence.** First, `resolve_stripped_job_config()` was silently deleting the `numa-bind` key before it ever reached `vllm serve` (since fixed). Second, even with that fixed and `numa-bind: true` confirmed present in `vllm.yaml.clean.yaml`, still zero `numactl`/`"Binding worker subprocess"` log output ever appeared (`logs/glm52q/20260811_121151/`) — traced to an upstream gap: `configure_subprocess()` (`vllm/utils/numa_utils.py`), the function that actually wraps a worker's launch in `numactl`, is only called from `v1/executor/multiproc_executor.py` and `v1/engine/utils.py` (the latter for `EngineCore` itself, regardless of executor backend). `v1/executor/ray_executor_v2.py` has zero references to it or to NUMA anywhere. Since this deployment requires `distributed-backend-executor: ray` for its multi-node topology, `numa-bind` cannot affect any of the 8 GPU worker processes here — confirmed structurally, not just by absent log lines. Drop it from future GLM-5.2 runs; it's dead weight for any Ray-executor deployment on this vLLM version (`0.26.0`).

  **Real bug found and fixed: `slurm-ray-vllm-serve.sh` was starving every node's Ray cluster of CPUs, and this fully explains the "usually slot 2" pattern below (now closed).** The two `srun --overlap` calls that launch `ray-setup.sh` (head start, worker start) requested `--cpus-per-task=72` — a *flat 72 CPUs for the entire node*, shared across all 4 GPU workers Ray spawns plus its own daemon overhead, versus the mp-path's `slurm-vllm-serve.sh` correctly using `--cpus-per-gpu=64` (256 total). This directly explained the recurring `ncclTopoGetCpuAffinity: Affinity for GPU N is empty, ignoring` warnings seen in every prior run: with only 72 of the node's ~288 real cores in the cgroup, NCCL's own topology auto-detection couldn't find a non-empty intersection between most GPUs' true NUMA-local range (e.g. `144-215` for GPU2) and the process's actual (uniformly `0-71`) affinity. Fix: `--cpus-per-gpu=72` (matching the confirmed real hardware topology — 4 GPUs × 72 cores = 288, an exact division — not `64`, which is unverified/unjustified and would still leave a partial-coverage gap; see conversation history for the full reasoning on why 72 beats 64 here). **Confirmed genuinely fixed** (`logs/glm52q/20260811_131631/` onward): `CPU affinity = 0-287` now, zero `"empty, ignoring"` warnings in any subsequent run, NCCL correctly self-resolves each GPU's true range (`GPU1 → 72-143`, `GPU3 → 216-287`, etc.).
  - **This did not fix the underlying hang** — identical signature persists post-fix.
  - **It did fully explain and close out the "usually slot 2" pattern**: earlier framing (below, retained for history) claimed the stuck rank was *always*/*usually* local GPU index 2, pointing at a fixed physical/positional cause. Once the CPU bug was fixed, the straggler position became effectively random — index 0, 1, and 2 have all been directly observed as the stuck slot across different post-fix runs (`20260811_125643`, `20260811_150319`, `20260811_154046`). Read together: "usually slot 2" was a symptom of *this* bug (whichever GPU's ideal NUMA range happened to be most poorly served by the too-small, always-`0-71` cgroup was most exposed), not an independent hardware/positional issue. That sub-thread is closed.
  - **Separately confirmed unrelated**: the long-standing, still-unresolved "4-node job only gets 2 nodes to come up" SLURM issue (`srun: Job step creation temporarily disabled, retrying`, investigated earlier via `diagnose-stuck-nodes.sh`/`ray-4node-smoketest.sh`, not yet written up in this file) was retested after this CPU fix and reproduces identically. Rules out this bug as an explanation for that separate problem — consistent with it having been seen on the mp path too, which never had this misconfiguration. That mystery remains fully open; `ray-4node-smoketest.sh` (built specifically for it) is still the recommended next step there, and hasn't been run yet as of this writing.

  **`max-num-seqs` 16→2 — no change to the hang, but read narrowly.** This tests *peak/concurrent* memory pressure (how many sequences run simultaneously), not *cumulative* memory churn since startup (how many total KV-cache blocks have been allocated/reused over the session's lifetime) — those are different variables, and only the first one was actually changed here. So this rules out "too many concurrent sequences" as the driver; it does **not** rule out cumulative churn, which is the version of the memory hypothesis that actually connects to a plausible mechanism (see below). Reverted back to `max-num-seqs: 16` afterward. (Side note while testing this: warmup step 1's `n: 4` parallel-completion request needs at least 2 sequential scheduling rounds under `max-num-seqs: 2`, which blew past the original `--max-time 10` on that curl call — bumped to `30`, unrelated to the hang itself but a real fix, see `run_vllm_warmup()` in `utils.sh`.)

  **Cumulative-churn hypothesis strengthened, single-request/conversation-length framing weakened.** `logs/glm52q/20260811_154046/` extended `run_vllm_warmup()` with a long-context warmup step that reached **142,019 tokens in one request** (54% of the 262144 `max-model-len`) — far past the ~15-25% range earlier runs' hangs had anecdotally correlated with — and did **not** hang immediately; three more substantial requests (`max_tokens=16324` each) served successfully over the next ~90s before the hang finally occurred. This argues against "any single request/conversation crossing some fraction of max-model-len" as the trigger (a single request blew straight through the earlier-inferred threshold with no immediate effect) and for a **cumulative total tokens/memory-churn-since-startup** framing instead — the real threshold is evidently higher than ~190K cumulative tokens in this run's case, not tied to one request's raw length. Exact cumulative threshold still unquantified. `observability_config.enable_logging_iteration_details` **is** a real internal `ObservabilityConfig` field (confirmed reachable and `True` in this run's effective config) but is not a plain CLI/yaml flag in its own right and produced no additional per-iteration scheduler logging in `vllm.0.log` even when set — not a useful lever as attempted; don't rely on it for future measurement.

  **Diagnostic findings that shifted or were retracted along the way**:
  - The "always/usually local GPU index 2" framing — see the CPU-allocation-bug section above; fully explained and closed, not an independent lead.
  - **`cxip_domain_enable(): Security Issue: Using unrestricted service ID 1 for cxi0-3`** appears on every NIC, every run, independent of the CPU fix — this job has no dedicated/isolated Slingshot service ID and shares the fabric's default one with whatever else is running on the cluster. Given the straggler position is now confirmed genuinely random (not explained by anything ivllm controls), this is the strongest remaining candidate for *why* it's random: external contention with other tenants' traffic on a shared, unisolated fabric resource has no reason to correlate with this job's own rank/topology layout. Not fixable from ivllm's side — needs an Isambard-admin-side dedicated `auth_key`/service ID. Worth escalating to support now given how much internal config-space has been exhausted without resolving the core issue.
  - **Libfabric log-level ordering is non-monotonic and genuinely counter-intuitive on this platform**: `FI_LOG_LEVEL=info` produced *more* detail than `trace` at startup, and — separately — produced **zero** additional output during the actual live hang window in `logs/glm52q/20260811_154046/` (every `NCCL INFO`/`libfabric:` line in that log is from initialization; nothing after warmup completed). So `info` only covers connection/init-time events for this provider, not live per-operation activity — not sufficient on its own to see what's happening (or not happening) at the moment of a stall. There is a further `debug` level above `info`, availability depends on how this libfabric build was compiled (not yet confirmed available). For live-hang visibility, targeted `FI_LOG_SUBSYS=cq,ep_data,mr` at `trace` (accepting the volume) remains the untried, most-likely-informative option. `NCCL_DEBUG` has stayed at `INFO` throughout this investigation — going to `TRACE` (filtered by `NCCL_DEBUG_SUBSYS=COLL,PROXY`, per the same volume-vs-signal tradeoff) is also still untried.

  **Open, not yet investigated**: in `logs/glm52q/20260811_121151/`, the head node's own `wait_report` monitoring loop (the periodic Cache/RAM/GPU/py-spy reporting) silently stopped producing output partway through the run, while the actual `EngineCore`/`APIServer` processes on that same node kept running and logging fine for another 6+ minutes. Cost the pyspy evidence needed to identify that run's actual straggler (node1's 4 workers were all already idle/waiting, meaning the true culprit was one of node0's 4 — invisible for this run). Did not recur in later runs (including ones on the fixed CPU allocation) — possibly transient/environmental. Not root-caused; worth a look if it recurs.

  **Possible-but-unconfirmed, noted for completeness, not a leading theory**: in `logs/glm52q/20260811_161429/` (also testing `VLLM_USE_AOT_COMPILE=1`, which otherwise looks like a non-factor — Triton's own JIT compilation still fires identically regardless), warmup step 2 timed out client-side (`--max-time 10` too tight for a still-genuinely-processing request; `APIServer` logs `"Aborted request(s)"` at the exact moment `curl` gave up) and the *very next* request — a trivial `n=4, max_tokens=5` ping — hung with the identical `Worker_TP6_EP6`-idle-while-siblings-spin signature, almost immediately, with no meaningful context or cumulative work involved. Raises the possibility that request cancellation/abort not fanning out synchronously across all 8 TP ranks could be *a* trigger path. **Explicitly not treated as the primary explanation**: this hang has been observed spontaneously, with no preceding cancellation of any kind, in many other runs throughout this investigation — so if abort is a trigger at all, it's at most one of several, not a necessary condition. Not pursued further for now in favour of the prefix-caching hypothesis below.

  ** `enable-prefix-caching`.** Prefix caching means KV-cache blocks are shared/reused across *multiple, otherwise-unrelated requests* over the session's lifetime (matched by prefix hash), not just allocated-and-freed once per request — a materially different and longer-lived form of block reuse than plain sequential allocation. This connects more specifically to the memory-registration-staleness mechanism already suspected (`FI_LOG_SUBSYS=mr`, discussed earlier) than raw cumulative token count does, and could plausibly explain the correlation with session length/total-requests-sent better than any single-request-size threshold has. Testing whether disabling it changes the hang's frequency/timing is the current active line of investigation.

  Prefix-caching disabled as a test: **no difference**. Ruled out alongside the abort/cancellation possibility above.

  **Leading theory as of 2026-08-11, found by reading the actual vLLM source at the exact hang point rather than continuing to vary external config**: a real, independently-documented weak-memory-ordering bug class in `vllm/distributed/device_communicators/shm_broadcast.py` — the exact module every single hang in this entire investigation (Nemotron, MiniMax-M3, GLM-5.2) bottoms out in (`EngineCore`'s `dequeue`/`acquire_read`, workers' `enqueue`/`acquire_write`). This module implements a hand-rolled, lock-free cross-process synchronization scheme over raw shared-memory metadata bytes (no OS locks), whose correctness depends entirely on a custom `memory_fence()` being called at exactly the right points:
  ```python
  def memory_fence():
      # Implementation acquires and immediately releases a lock. Python's
      # threading.Lock provides sequentially consistent memory barrier semantics
      # across all major platforms (POSIX, Windows).
      with _memory_fence_lock:
          pass
  ```
  That documented assumption — an ordinary process-local `threading.Lock` providing sufficient cross-process, cross-core memory-barrier semantics — is safer on x86 (strong TSO ordering, where vLLM's development/CI is overwhelmingly hosted) than on **aarch64**, which is what every GH200 node's Grace CPU is.

  **This is not speculation from reading code in isolation — it's a confirmed, real, previously-reported bug class in this exact file, on this exact CPU architecture**, found via `git log` on `vendor/vllm` (full clone, all tags) plus a web search once the pattern was clear:
  - [`vllm-project/vllm#27858`](https://github.com/vllm-project/vllm/issues/27858) — filed independently by a real user, explicitly on **aarch64** (`Architecture: aarch64` in their environment report), describing this exact gap in their own words: *"Before `metadata_buffer[0] = 1` is set, we should make sure that `buf` is synchronized across processes."* Their symptom was sporadic pickle/deserialization corruption rather than a clean hang, but the same root mechanism — a reader observing the "ready" flag before the buffer contents are actually visible to it.
  - `PR #30407` ("fix(shm): Add memory barriers for cross-process shared memory visibility", Dec 2025) — introduced `memory_fence()` from scratch; almost certainly the direct response to `#27858`.
  - `PR #32022` ("[Bugfix] Fix memory inconsistency in cross-process shared memory", Feb 2026) — a **second**, separately-needed fence added just ~2 months later, at a spot the first fix missed, with its own commit comment explicitly citing "CPU architectures with weak ordering."
  - `PR #28053` ("[Core] Remove busy loop from idle buffer readers", March 2026) — a large (258-line) rewrite of the *entire waiting mechanism* immediately after both fence fixes, replacing a simpler busy-loop with the current ZMQ-notification-based `SpinCondition` class. This newer code was never specifically re-audited for the same class of issue the two fence PRs were written and tested against. **Correction, found via `VLLM_USE_SPINLOOP_EXT` research**: #28053 only removed the *idle* busy-spin — [PR #36517](https://github.com/vllm-project/vllm/pull/36517) ("Efficient Busy Polling Optimization") confirms, via a direct maintainer exchange, that **a busy-poll path still exists under load** in the current code (the PR author verified `monitorx`/`mwaitx` power savings "under load," which only makes sense if busy-polling is still happening then). So the notification-vs-busy-poll picture is more nuanced than "idle readers now sleep, done" — there's still a busy-poll code path active under some conditions, worth understanding before assuming the ZMQ-notify rewrite fully replaced spinning everywhere.

  Both fence fixes are present in our installed `0.26.0`, confirmed by direct read of the vendored source — so the *already-known, already-reported* bug is fixed. But the vLLM team needing two separate passes to get the fencing right on their first, simpler implementation, followed by a substantial rewrite of the surrounding logic that was never re-audited for the same issue, makes a third, still-undiscovered gap in the newer notification path entirely plausible — and this is the first explanation across the whole investigation that would account for *why* nothing at the NCCL or libfabric level has ever shown an anomaly during a live hang: the bug wouldn't be in the network or GPU collective path at all, it'd be in this host-CPU shared-memory IPC mechanism between `EngineCore` and its workers, which every diagnostic tried so far (pyspy, NCCL logs, libfabric traces) has been looking *around*, not *at*.

  **Patchable, pure Python, no recompilation needed** (`vllm/distributed/device_communicators/shm_broadcast.py`) — same `apply-vllm-patch.sh` mechanism already used for `hybrid-trtllm.sh`/`disable-flashinfer-env.v0.25.1.patch` applies directly. **Decided to bottom this out further before reporting upstream, rather than filing on the strength of the code-reading + precedent alone.** Concrete next steps for whoever picks this back up:
  - Read `PR #28053`'s full diff and its own `test_shm_broadcast.py` additions closely — check whether the new `SpinCondition`/ZMQ-notify path has its own explicit "subscriber ready" handshake analogous to the data-channel's `wait_until_ready()`, or whether there's a ZMQ PUB/SUB "slow joiner" race (a message sent before a SUB socket's background connection handshake completes is silently dropped) — this would be a *second*, independent bug in the same file, distinct from the memory-fence question, also worth ruling in or out.
  - Try strengthening `memory_fence()` beyond the `threading.Lock` implementation — e.g. an explicit hardware barrier via a small `ctypes` call to `__sync_synchronize`/a compiler atomic-fence built-in — and see if hang frequency/timing changes at all. A change either way is informative.
  - If any x86 GPU hardware is available anywhere (even outside Isambard), check whether the identical config reproduces the hang there at all, or takes measurably longer to. Strong x86 TSO ordering could plausibly mask this class of race almost entirely, making a clean x86/aarch64 comparison close to differential proof.
  - Once reasonably confident, file upstream referencing `#27858`/`#30407`/`#32022`/`#28053` directly and our own symptom (GH200/aarch64, multi-node Ray executor, response never arriving) — this is a core, widely-shared vLLM mechanism, not something specific to this deployment, so a confirmed residual gap would be valuable well beyond this project.

  **Confirmed via `git fetch --tags` on `vendor/vllm` (2026-08-11): the file has been touched again since `0.26.0`, and two of the four commits between `0.26.0` and the now-available `0.27.1` look like an extremely strong match, independent of the memory-fence theory above.**
  - [`10c75477b`](https://github.com/vllm-project/vllm/commit/10c75477b) — "shm_broadcast: bound idle reader waits and release read slots" (#45224, July 2026). Two fixes in one: (a) previously, an idle reader waiting indefinitely with no active warning/deadline could get a `None` poll timeout on the ZMQ notify socket — an infinite wait with **zero periodic fallback recheck** of the actual SHM flag. If that one notification is ever lost (a real risk in the ZMQ-`SpinCondition` mechanism `#28053` introduced), the reader hangs forever even though the data was already correctly written. Fixed by capping every wait at a new `SHM_READER_RECHECK_INTERVAL_MS = 5000` unconditionally. (b) the read-completion flag was previously set *after* `yield buf` with nothing guaranteeing it ran if the caller raised mid-processing — wrapped in `try`/`finally`. This second half directly matches the abort/cancellation observation noted above: an exception while handling a cancelled request's response could permanently strand a reader's slot as "not read," leaving the writer waiting forever for a completion that could structurally never happen.
  - [`48aa8d8d7`](https://github.com/vllm-project/vllm/commit/48aa8d8d7) — "Prevent stale multiproc RPC deadlines from becoming unbounded waits" (#41357, July 2026). A sign bug: `deadline - time.monotonic()` going slightly negative (any small scheduling delay past an intended deadline) produced a negative timeout passed to `zmq.Socket.poll()`, which treats negative timeouts as *block indefinitely* (`-1` is libzmq's own convention) rather than *already expired*. Fixed via `max(0, ...)` clamping in both `shm_broadcast.py` and `multiproc_executor.py`.
  - The other two commits in this range (`831d3848f`, fail-fast on undersized `/dev/shm`; `a07fac758`, zero-copy tensor pickling perf) are unrelated to hangs specifically.
  - **Both `10c75477b` and `48aa8d8d7` describe exactly the symptom shape observed in every run of this investigation** (one side genuinely finished, the other waiting indefinitely for a notification/deadline check that never resolves) — at least as strong a candidate as the memory-fence theory, possibly stronger given how precisely "lost notification, no recheck fallback" matches the pyspy evidence. **Next step, not yet done**: cherry-pick these two commits as a patch onto the installed `0.26.0` (via `apply-vllm-patch.sh`, isolating exactly these fixes) rather than a full version bump to `0.27.x`, so a test result is attributable to these specific changes and not confounded by anything else that moved between versions.

  **`shm-broadcast-lost-notify-fix.v0.26.0.v1.patch` (`10c75477b`+`48aa8d8d7`) tested — did not prevent the hang, but the way it failed is itself informative.** `logs/glm52q/20260811_222935/`: same signature, EngineCore stuck in `dequeue`/`get_response`, all of node0's workers at `_to_list`. Since this patch specifically caps idle-reader waits at 5s and fixes the negative-timeout misinterpretation, a hang persisting for 4+ minutes past that means the fix's target mechanism ("data ready, notification lost") genuinely isn't what's happening here — the response was never written in the first place. Doesn't kill the `shm_broadcast.py` theory in general (a different residual gap in the same file is still plausible, see the "bottom out further" checklist above), but rules out this specific pair of upstream fixes as sufficient. Kept applied going forward regardless, since it's a real, independently-justified upstream fix either way.

  **Built `shm-broadcast-stuck-queue-diagnostics.v0.26.0.v2.patch` to stop inferring the stuck rank from GPU utilisation and read it directly instead** (see `design/prototype/patch/README.md` for the full patch description). Enriches the existing `"No available shared memory broadcast block found"` warning with the actual ring-buffer metadata (`written_flag`, read flags, a human-readable queue label) every time it fires. Two runs in:
  - `logs/glm52q/20260811_231350/`: `written_flag=1, own_read_flag=1` — the ring buffer's own documented "case 4" state (written and fully read by all readers). Confirms `EngineCore` had already successfully received and consumed that worker's *last* response and was correctly, normally waiting for the *next* one — not a lost-notification bug, just genuine silence from one worker.
  - `logs/glm52q/20260811_233628/` (v2, after fixing the label to also cover `RayExecutorV2`'s own queue-construction path, separate from `MultiprocExecutor`'s — see patch README): **`queue=response[rank=0]`, definitively.** Rank 0 lives on node0, where all four GPUs read 100% the entire time — not the visually-obvious 0%-util rank (node1's `gpu0`, global rank 4) this run.

  **This retires the "0%-GPU-util rank is the stuck one" heuristic used through most of this investigation — it was backwards.** The 0%-util rank is the one that finished first and is correctly idling; the actual blocker is hiding among the still-100%-spinning ranks, and this is now confirmed with hard, code-level evidence rather than inferred from ambiguous pyspy snapshots (which still can't distinguish it from its siblings on their own — all four of node0's workers showed the identical `_to_list` frame in this same snapshot). Any earlier attribution in this document naming a specific "stuck" rank/slot based on GPU utilisation alone should be read with this caveat. Going forward, the diagnostic patch is the reliable way to identify the actual culprit, not GPU util.

  **New hypothesis, not yet verified: proximity to a Triton JIT compilation event during inference.** In the `20260811_233628` run, rank 0's last own log line before going silent was a `"Triton kernel JIT compilation during inference: _compute_slot_mapping_kernel"` warning at `23:32:17` — consistent with a pattern noticed across most runs this investigation. Worth being precise about the actual timing here rather than overstating it: in this run, ~75 seconds of *successful* serving (7 real completions) followed that specific warning before the hang actually locked in at `23:34:32` — so it isn't an immediate, tight causal trigger in this instance, at least not on the very next step. Plausible mechanism if the correlation holds up under closer scrutiny: a JIT compile shells out to `ptxas`/a C compiler as a real subprocess (confirmed elsewhere in this project's investigation history, see the MiniMax-M3 entry below) and can genuinely stall the compiling rank for seconds; if that stall exceeds some internal timeout/heartbeat window, the other 7 ranks could plausibly move on or otherwise diverge from that rank's notion of the current collective epoch, and the compiling rank — once it finishes — ends up trying to rejoin a collective the group has already abandoned, producing exactly the kind of permanent, one-sided wait observed. Not yet tested; next steps (checking whether every hang has a JIT-compile event on the eventual culprit rank within some consistent preceding window, and whether the gap-to-hang duration is roughly consistent across runs) will have to wait for later.

  **Lead from libfabric's own manual, investigated and closed: a documented "CUDA deadlock" mechanism exists, but GDRCopy was the wrong tree for CXI specifically.** Per `fi_endpoint(3)`/the libfabric manual: `cudaMemcpy()` calls made *inside libfabric itself* can deadlock when a CUDA kernel blocks waiting on a host-side `cudaMemcpy` to complete — a real, named failure mode, not this project's own speculation. Two documented mitigations exist: (1) GDRCopy (`NCCL_GDRCOPY_ENABLE`/`FI_HMEM_CUDA_USE_GDRCOPY`); (2) `FI_OPT_CUDA_API_PERMITTED=false`, an `fi_setopt(FI_OPT_ENDPOINT, ...)` call restricting an endpoint from making CUDA API calls at all — **not usable from ivllm today**, since it's an application-code API call, not an environment variable, and it isn't confirmed whether `aws-ofi-nccl` (the actual libfabric caller in this stack) exposes an equivalent env-settable knob. GLM-5.2's longest-surviving run happened to flip `NCCL_GDRCOPY_ENABLE`/`FI_HMEM_CUDA_USE_GDRCOPY` to `1` alongside the `PHB` change (not isolated from that test, so still unconfirmed either way) — three follow-up findings now rule this out entirely rather than just downgrade it: (a) per `fi_cxi(7)` (`design/references/fi_cxi.md`, confirmed direct quote): *"the CXI provider enables ... FI_HMEM_CUDA_USE_DMABUF by default if not specifically set"* — DMABUF is a different zero-copy mechanism that also avoids a host-side `cudaMemcpy` in the transfer path, so CXI already sidesteps this deadlock class by default, without GDRCopy, and `fi_cxi(7)` never mentions GDRCopy at all; (b) per `nccl.md` (`design/references/nccl.md`): `NCCL_DMABUF_ENABLE` defaults to `1` (enabled) at the NCCL layer too, independent of CXI — a second, separate default pointing at DMABUF; (c) `NCCL_GDRCOPY_ENABLE` itself does not appear anywhere in that same 102-variable NCCL reference, a real question mark over whether it does anything at all in whatever NCCL build ivllm runs. Separately, there's a real, maintainer-confirmed, CXI-specific race condition in the GDRCopy cleanup path — [ofiwg/libfabric#10041](https://github.com/ofiwg/libfabric/issues/10041), a double-free when two threads unmap/unpin the same GDR handle, reported on multi-node Slingshot 11 (this project's exact fabric), maintainer quote: *"very much tied to the cxi provider"* — looks stale (last real human activity Aug 2024) rather than deliberately ruled out, no workaround documented. Net: every layer of this stack already defaults to DMABUF without needing GDRCopy; explicitly enabling GDRCopy adds a live, unresolved crash risk with no confirmed benefit. **Closed — not a fix candidate.** See `knowledge-base.md`'s Mandatory table for the full sourcing.

  **Live run, 2026-08-12, `logs/glm52q/20260812_213446/`: `disable-custom-all-reduce: true` tested — does NOT fix the hang, but this run's diagnostics finally pinpoint the exact freeze location, and it reframes the whole investigation.** Same signature as ever operationally (exit 254, user abort after ~13 min, `[ivllm-diag]` shows `queue=response[rank=0]` stuck, first at `written_flag=0/own_read_flag=0` during a transient warmup-time stall that resolved, then permanently from ~21:31:20 with `written_flag=1/own_read_flag=1` — the "already fully consumed, EngineCore correctly waiting for the next response" state, not a lost-notification bug). **#8410's custom-all-reduce hang mechanism is now ruled out for this specific hang** — disabling it changed nothing.

  **The new, decisive evidence is in the pyspy dumps.** At 21:31:24 (just as the hang locks in) and again at 21:32:17, 21:33:something, and 21:34:13 (unchanged, over 3 minutes later — a genuine freeze, not a coincidentally-sampled fast path), **all 8 GPU workers, both nodes, are frozen at the identical Python frame**:
  ```
  _to_list (vllm/v1/worker/gpu_model_runner.py:7769)
  _bookkeeping_sync (vllm/v1/worker/gpu_model_runner.py:3727)
  sample_tokens (vllm/v1/worker/gpu_model_runner.py:4669)
  ```
  Read directly from `vendor/vllm-0.26.0`: line 7769 is `self.transfer_event.synchronize()` inside `_to_list()` — a `torch.Event()` recorded on the current (default) CUDA stream right after a non-blocking pinned D2H copy of the sampled tokens. The function's own comment explains why it exists: *"`tolist` would trigger a cuda wide stream sync, which would block other copy ops from other cuda streams... A cuda event sync would avoid such a situation"* — citing [vllm-project/vllm#22754](https://github.com/vllm-project/vllm/issues/22754) as the original motivating issue. Per `_bookkeeping_sync`'s own comment a few lines up: this event "waits for every D2H queued on the default stream since the last sync" — i.e. `transfer_event.synchronize()` blocks the Python thread until **everything else already queued on that rank's default CUDA stream this iteration has finished**, including every attention/MoE/allreduce kernel issued earlier in the same forward pass. Fetched #22754 directly: it documents the original stream-blocking problem and the event-based fix, but **contains no discussion of whether the event-based fix can itself hang** — this specific failure mode looks genuinely unreported upstream.

  **This reframes the entire investigation.** `shm_broadcast.py` (memory fences, `SpinCondition`, lost notifications — all the prior theories) is not where the bug lives — it's just the mechanism by which EngineCore *reports* that a worker never sent a response. The worker never gets that far because it's frozen **inside its own forward pass**, at a point that only blocks if an *earlier* kernel on that same GPU's default stream never completes. Since all 8 ranks show the identical frozen state simultaneously (not staggered, not diverging), the most coherent explanation is a genuine cross-rank NCCL/collective desync somewhere in the forward pass (TP allreduce or MoE/EP dispatch-combine) — some rank never receiving or sending its side of a collective — which stream-orders into every rank's own `_to_list()` call hanging behind it. This is the second confirmed occurrence of this *exact* frame (also seen in `20260811_233628`, both times localized to node0/`queue=response[rank=0]`) — a real, reproducible, precise signature, not a one-off.

  **This directly validates and reprioritizes the `TORCH_NCCL_DESYNC_DEBUG`/flight-recorder recommendation above** — that diagnostic exists specifically to answer "which rank/collective is desynced," which is now clearly the right question to be asking, more so than continuing to audit `shm_broadcast.py`'s IPC layer. It should be the next thing tried, not one item among several.

  **Verified: NCCL's own verbose logging (`NCCL_DEBUG=INFO`) is exhausted as a diagnostic avenue here too, for the same reason as libfabric's.** Checked every rank's last logged NCCL line in this run: all 8 (both nodes) logged an `AllReduce`/`AllGather` launch in the same narrow window right before the freeze (~21:31:10–21:31:20) — consistent with the pyspy evidence that every rank was still mid-forward-pass at that point — then **zero further NCCL log lines from any rank for the entire ~3.5-minute hang** (out of 9825 total NCCL log lines in the run). `NCCL_DEBUG=INFO` only logs at collective *launch*, not while a rank is blocked waiting inside one — so once something stalls mid-collective there's structurally nothing further to log. Confirmed exhausted, not worth turning up further (proven, this run).

  **Important gotcha found for the `TORCH_NCCL_DESYNC_DEBUG`/flight-recorder plan above: it likely wouldn't have fired yet in any run so far.** vLLM's own default `--distributed-timeout-seconds` (the `torch.distributed.init_process_group` timeout for NCCL groups) is **600 seconds** (per `vllm-serve-cli.md`). `TORCH_NCCL_DESYNC_DEBUG`/the flight recorder's dump-on-timeout mechanism only activate when PyTorch's own NCCL watchdog detects a timed-out collective — i.e. only after that full 600s window elapses. This run's confirmed hang window (last successful activity ~21:31:20 to the manual abort at 21:34:45) was only ~3.5 minutes/~210s — well short of 600s. **Every hang so far has likely been manually cancelled before the watchdog ever got a chance to fire**, meaning `TORCH_NCCL_DESYNC_DEBUG=1` alone, set the same way every other env var has been, would produce nothing regardless of whether it's "working." For the next run: (1) set the full diagnostic set together — `TORCH_NCCL_DESYNC_DEBUG=1`, `TORCH_NCCL_TRACE_BUFFER_SIZE=2000` (must be `>0` for anything to populate), `TORCH_NCCL_DUMP_ON_TIMEOUT=1`, `TORCH_NCCL_TRACE_CPP_STACK=1` — `DESYNC_DEBUG` alone won't populate the flight recorder without the trace buffer being sized; (2) also set `TORCH_NCCL_DEBUG_INFO_PIPE_FILE=<job debug dir>/torch_nccl_dump_trigger` so a dump can be **manually forced** the moment the existing fast (~60s) `"No available shared memory broadcast block"` warning appears, by writing anything to that pipe — this sidesteps the 600s wait entirely; (3) if manual triggering proves awkward, fall back to patience — let the job sit stuck for at least ~11 minutes past the point it looks hung before running `ivllm cancel`, so the automatic on-timeout dump gets its full window (documentation for the default timeout; inferred for why prior attempts, if any, would have produced nothing).

  **GitHub research pass across vLLM's own issue/PR history (2026-08-12) — several independent, near-identical community hang reports found, plus one immediately-testable gap in our own config.** Ran a systematic check of every candidate env var reviewed this session (~55 vars across NCCL/libfabric, CUDA driver, `TORCH_NCCL_*`, PyTorch-CUDA, and Triton) against vLLM's GitHub. Full per-variable results are folded into `knowledge-base.md`; the findings that matter for *this* investigation specifically:

  - **[vllm-project/vllm#45198](https://github.com/vllm-project/vllm/issues/45198)** — "vllm hangs at Triton kernel JIT compilation during inference: `_zero_kv_blocks_kernel` with TP=2" (open, unresolved). Workers stuck at `load_binary START`, EngineCore reports **"no available shared memory broadcast blocks for 60+ seconds."** This is independent, external confirmation of the exact JIT-compile-proximity mechanism already hypothesized above — a Triton JIT stall manifesting *as* an `shm_broadcast` timeout. Their workaround: `--disable-custom-all-reduce`. References a sibling open bug, **#33041**.
  - **Immediately actionable, cheap gap found by comparing our own configs**: **[#8410](https://github.com/vllm-project/vllm/issues/8410)** "Obvious hang caused by Custom All Reduce OP" — one rank hangs inside vLLM's custom all-reduce kernel while the others wait; fixed by `--disable_custom_all_reduce`. **`disable-custom-all-reduce: true` is already set in three other multi-node configs in this repo** (`nemotron-3-ultra-550B-A55B-FP8.yaml`, `qwen3.5-397b-a17b-fp8.yaml`, commented-out in `minimax-m3.yaml`) — **but not in `glm-5.2-743b-int4.yaml`**, the one config actually under active hang investigation. Directly analogous in cost/risk to the already-proven `NCCL_NET_GDR_LEVEL=PHB` test. **Not yet tried.**
  - **[#29544](https://github.com/vllm-project/vllm/issues/29544)** — `PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True` throws `RuntimeError: cancelled` from **`shm_broadcast.acquire_read`** itself during multi-node TP4 EngineCore init, → SIGABRT. Different trigger (allocator config, not JIT), but independent confirmation `shm_broadcast.acquire_read` is a real, reachable failure point under multi-node TP in vLLM's own bug tracker — read against our own `shm_broadcast.py` "bottom out further" checklist above. (Not applicable as a fix for us either way — `expandable_segments` isn't set anywhere in ivllm.)
  - **`VLLM_USE_V2_MODEL_RUNNER=0` re-examined**: the vLLM issue we originally tried this workaround for, **#43420**, has a root cause specific to **data-parallel** workers crossing node boundaries (falls back to Gloo-over-TCP, times out) — GLM-5.2 runs pure TP with no DP split, so that precondition likely never applied here, which would explain why the workaround changed nothing. **#44697** separately reports V2 getting silently force-re-enabled in some flag combinations regardless of the override — worth confirming from logs that GLM-5.2's runs actually executed on the legacy V1 runner, given this project's history of silently-ignored flags (`enable-flashinfer-autotune`, the `VLMM_LOGGING_LEVEL` typo).
  - **Exact-phrase match, already confirmed in our own logs**: this document's own GLM-5.2 entry above already records EngineCore logging *"No available shared memory broadcast block found in 60 seconds"* — this is the **identical message** (down to near-exact wording) reported independently in **[#51593](https://github.com/vllm-project/vllm/issues/51593)** (DeepSeek-V4-Flash, MTP, deterministic at batch=3, unresolved, root cause unknown) and #45198 above. Three unrelated models/deployments hitting the same message is real corroborating evidence this is a genuine, reproducible vLLM-internal failure surface, not something specific to GLM-5.2 or this platform.
  - **`NCCL_CUMEM_ENABLE=0`** (currently set via `vllm-env.sh`, filed as "unproven value") is actually a historical, well-documented fix for an NCCL 2.19/CUDA-graph bug ([vllm-project/vllm#5091](https://github.com/vllm-project/vllm/pull/5091)), later **reverted upstream** once NCCL 2.22.3 fixed it ([#24141](https://github.com/vllm-project/vllm/pull/24141), which notes forcing `=0` now costs real performance). This platform already runs **NCCL 2.30.4** (confirmed via `nccl-probe.sh`, well past 2.22.3) — the original justification for `=0` may no longer hold here. Worth a deliberate re-test, watching for the separate `NCCL_CUMEM_ENABLE=1` + symmetric-memory graph-capture crash reported in [#28901](https://github.com/vllm-project/vllm/issues/28901) (possibly the same interaction already forcing `VLLM_ALLREDUCE_USE_SYMM_MEM=0`).
  - **A genuinely novel, purpose-built diagnostic with zero vLLM-community precedent**: `TORCH_NCCL_DESYNC_DEBUG` (PyTorch's own description: "helpful in figuring out the culprit rank of collective desync") plus the flight-recorder pair `TORCH_NCCL_DUMP_ON_TIMEOUT`/`TORCH_NCCL_TRACE_BUFFER_SIZE`. This is close to exactly what the manual `shm-broadcast-stuck-queue-diagnostics.v0.26.0.v2.patch` work above was built by hand to answer ("which rank is actually stuck"), but built into PyTorch itself. Nobody in vLLM's issue tracker appears to have tried it — trying it here would be new ground, not retreading a known dead end. See `ivllm-environment.md`'s "Debugging flags" section (Level 4) for the full variable list — now prototyped in `design/prototype/debug-monitor-prototype.sh`.
  - **Other community reports with a closely-matching failure shape, worth reading directly**: [#46097](https://github.com/vllm-project/vllm/issues/46097) (MoE multi-node TP=4 deadlock, all ranks spinning ~96% SM/0% memory movement — workaround: `VLLM_EXECUTE_MODEL_TIMEOUT_SECONDS` + external watchdog + `py-spy dump --native`), [#41725](https://github.com/vllm-project/vllm/issues/41725) (shm-broadcast timeouts, both nodes pinned 96–99% util, unresolved), [#6042](https://github.com/vllm-project/vllm/issues/6042)/[#12625](https://github.com/vllm-project/vllm/issues/12625) (a rank crashing with illegal memory access *inside an MoE layer* while other ranks hang in allreduce — different root cause, worth ruling out given GLM-5.2 is MoE), [#18431](https://github.com/vllm-project/vllm/issues/18431) (engine stuck, request/KV-cache counters frozen — matches our EngineCore-stuck-forever shape), [#29740](https://github.com/vllm-project/vllm/issues/29740) (shared-memory-broadcast-timeout hang under TP on dual RTX Pro 6000D).
  - **Also confirmed as a real vLLM-developer technique, worth trying next reproduction**: `CUDA_LOG_FILE=stderr` — used live by vLLM devs to unmask detailed CUDA driver errors underneath otherwise-opaque NCCL/CUDA-graph failures ([PR #29207](https://github.com/vllm-project/vllm/pull/29207), [#49826](https://github.com/vllm-project/vllm/issues/49826)), explicitly gated to CUDA 12.9+ — matches this stack's forward-compat baseline exactly.

  **Follow-up GitHub research pass (2026-08-12), scanning `vllm-env.md`/`vllm-serve-cli.md` for further stability-relevant flags — several more close analogs and one confirmed-safe finding:**

  - **[#40926](https://github.com/vllm-project/vllm/issues/40926)** "V1 engine + MTP + GLM-5.1 (DSA + MoE + MLA) — workers hang under sustained traffic, `sample_tokens` RPC timeout, EngineDeadError" (open). Same model family, **TP=8, EP=true** (identical parallelism shape to our GLM-5.2 INT4 config), MTP speculative decoding, `--disable-custom-all-reduce` already set. Hangs under *sustained low-rate* traffic (~0.2 RPS), not a burst — traced to `shm_broadcast.py`'s `acquire_read`/`_spin_condition.wait()` timing out; three candidate causes floated (MTP deadlock, DSA/MTP interaction, MoE+MLA+MTP combo in V1's async pipeline). Fix: disable speculative decoding entirely. **Checked our own configs**: `glm-5.2-743b-int4.yaml` (the config under active investigation) has no `speculative-config` set, so this exact root cause doesn't explain the current hang. `glm-5.2-743b.yaml` (the separately-tracked FP8 variant, blocked on 4-node job access per `priorities.md`) *does* set `speculative-config: {"method": "mtp", "num_speculative_tokens": 5}` — worth watching for this signature specifically (sustained low-rate traffic, RPC timeout, not burst-load) once 4-node FP8+MTP testing resumes.
  - **[#41530](https://github.com/vllm-project/vllm/issues/41530)** "TP Worker hang causes EngineDeadError - RPC call to sample_tokens timed out" (DeepSeek-V4-Pro, TP=8, MTP) — shared-memory broadcast components can't locate available blocks, RPC times out, TP worker hangs **silently with no exceptions logged**. Close structural analog (TP=8, shm_broadcast, MoE).
  - **[#17676](https://github.com/vllm-project/vllm/issues/17676)** — a **fourth** independent report of the identical error message our own logs already show (*"No available shared memory broadcast block found in 60 second[s]"*, alongside #51593 and #45198) — strong, repeated evidence this is a generic, reproducible vLLM-internal failure surface.
  - **[#42742](https://github.com/vllm-project/vllm/issues/42742)** — PyTorch's own NCCL heartbeat watchdog (`TORCH_NCCL_HEARTBEAT_TIMEOUT_SEC`, default 600s) can SIGABRT a worker after a long *idle* period with no preceding log line — a failure layer vLLM's own `--*-timeout-seconds` flags don't cover at all. **Worth checking directly against our own hang logs**: do GLM-5.2 hangs correlate with an idle gap before a traffic burst, or happen under continuous sustained load? Not yet checked.
  - **[#27774](https://github.com/vllm-project/vllm/issues/27774)** (fault-tolerant EP RFC) states plainly that some EP kernels "will hang indefinitely" on rank failure — i.e. today's vLLM fault-tolerance mechanisms don't prevent our exact hang class; no fix exists upstream for this yet.
  - **`VLLM_USE_RAY_V2_EXECUTOR_BACKEND` — confirmed via direct read of our vendored `vllm-0.26.0` source (`vendor/vllm-0.26.0/vllm/envs.py`) to default to `True`.** This means GLM-5.2's Ray-executor deployment is already running on **`RayExecutorV2`** — the exact component #43420's hang lives in — regardless of the DP-specific precondition question raised earlier. A vLLM/Ray-team RFC ([#35848](https://github.com/vllm-project/vllm/issues/35848)) states the *old* backend (`RayDistributedExecutor`/Compiled Graph) had "unresolved stability issues including NCCL hangs and out-of-order delivery across multiple Ray releases," which is why `RayExecutorV2` was built — so this isn't a case of picking between a known-good and known-bad backend, both have documented hang histories. Untested here: forcing `VLLM_USE_RAY_V2_EXECUTOR_BACKEND=0` to fall back to the older backend, as a diagnostic (not necessarily a fix, given its own known issues).
  - **`VLLM_DISTRIBUTED_USE_SPLIT_GROUP`** — real external (non-vLLM) hang mechanism found: [pytorch/pytorch#145376](https://github.com/pytorch/pytorch/issues/145376) reports `torch.distributed.new_group` with the Gloo backend hanging when `split_group` was called first and not all ranks belong to the resulting group. This flag is exactly the toggle between vLLM's `split_group`-based subgroup construction (mixed `cpu:gloo,cuda:nccl` backend + eager `device_id` binding, per its own source comment) and the legacy `new_group` path — a plausible mechanism for a cross-node hang if vLLM's subgroup construction mixes ranks inconsistently across TP/EP. Wouldn't show up in vLLM's own issue tracker since it's a PyTorch-level bug — worth testing this flag directly rather than searching for more corroboration.
  - **`VLLM_USE_SPINLOOP_EXT`/PR #28053 clarification**: see the correction added to the `shm_broadcast.py` notes above — a busy-poll path still exists under load even after #28053's rewrite.
  - **Verified NOT a risk here**: research raised a concern that Ray driver-to-worker env var copying (`VLLM_RAY_EXTRA_ENV_VAR_PREFIXES_TO_COPY`, built-in defaults only `VLLM__`/`PYTHONHASHSEED`) could mean our many non-`VLLM_`-prefixed tuning vars (`NCCL_*`, `FI_*`) silently never reach Ray worker processes on other nodes — a real failure mode documented independently in [vllm-project/vllm#6803](https://github.com/vllm-project/vllm/issues/6803). **Checked directly against `src/engine/lib/ray-setup.sh`**: this script runs independently via `srun` on *every* node (not just the head), and `eval "$envExports"` (the job's custom `env:` block) plus `common-env.sh`/`vllm-env.sh` all run *before* `ray start` on that same node (lines 48–61, `ray start` at line 95). Every node's Ray daemon already has every custom env var baked into its own process environment before Ray even starts there — ivllm's architecture sidesteps the Ray-copy mechanism entirely rather than depending on it. Closed, not a live risk.
  - **Confirmed already safe**: `VLLM_WORKER_MULTIPROC_METHOD` defaults to `fork`, and vLLM's own maintainers identified fork+Ray-actor as undefined behavior (PR #14705) — but `vendor/vllm-0.26.0/vllm/utils/system_utils.py`'s `_maybe_force_spawn()` explicitly detects `is_in_ray_actor()` and forces `spawn` regardless of the default, confirmed present in our installed version. Nothing to change here.

  **First real `IVLLM_DEBUG_LEVEL=4` run, 2026-08-14 (`logs/glm52q/20260814_203641/`) — the flight-recorder trigger mechanism works, but the captures themselves were empty. Two bugs found and fixed in `design/prototype/debug-monitor-prototype.sh` (v2), not yet re-tested.** `monitor_head`'s stall-detection correctly fired 3 times at the intended ~5-minute cooldown spacing, and writing to the per-rank trigger pipes reliably produced `"ProcessGroupNCCL preparing to dump debug info"` in the log — the trigger design itself is sound. But every dump that landed decoded to `{'entries': [], 'pg_config': {}, 'pg_status': {}}` — completely empty. Root causes, both confirmed directly from the log/filesystem, not inferred: (1) **three `TORCH_NCCL_*` names are deprecated in this PyTorch build** (v2.10) — `TORCH_NCCL_TRACE_BUFFER_SIZE`/`TORCH_NCCL_TRACE_CPP_STACK`/`TORCH_NCCL_DEBUG_INFO_TEMP_FILE`, superseded by `TORCH_FR_BUFFER_SIZE`/`TORCH_FR_CPP_STACK`/`TORCH_FR_DUMP_TEMP_FILE` — the old names are read (no error) but evidently not honored for effect, so the flight-recorder ring buffer was never actually sized; (2) `set_debugging_env()` read `$SLURM_NODEID` directly instead of taking an explicit node-rank parameter the way `report_memory()`/`wait_report()` already do, and on this run that resolved to `0` on both physical nodes — confirmed via `ls debug/`, which showed all 8 rank pipe files sharing one `node0`-prefixed path, never any `node1*` variant. Both fixed in the prototype's v2 (sets both old and new `TORCH_FR_*`/`TORCH_NCCL_*` names together; `set_debugging_env()` now takes an explicit node-rank arg). Still unexplained: only 4 of 8 expected trace-dump files landed on disk (`node00`-`03`, never `04`-`07`) even though log evidence suggests more ranks received the trigger by the third attempt — worth rechecking once the node-rank fix is applied, in case it was the same shared-path collision rather than a separate issue.

  **Status**: active as of 2026-08-12. `disable-custom-all-reduce: true` tested and **ruled out** (see the live-run entry above) — the pyspy evidence now points squarely at a cross-rank NCCL/collective desync inside the forward pass, surfacing as a frozen `transfer_event.synchronize()` call (`gpu_model_runner.py:7769`) on every rank simultaneously, with `shm_broadcast.py` merely reporting the resulting silence rather than being the root cause itself. Revised priority order: (1) **re-run with `design/prototype/debug-monitor-prototype.sh` v2 applied** — the flight-recorder trigger mechanism is confirmed working, it just needs the two bugs above fixed (deprecated `TORCH_NCCL_*` names, shared node-rank path) before it can produce a real, non-empty capture; still the clear #1, since a genuinely populated flight recorder is the most direct way to answer "which collective, which rank" rather than further `shm_broadcast.py` IPC-layer auditing; (2) verify from logs that `VLLM_USE_V2_MODEL_RUNNER=0` actually took effect rather than being silently overridden; (3) test `VLLM_DISTRIBUTED_USE_SPLIT_GROUP=1` given the independent PyTorch Gloo/split_group hang mechanism (pytorch/pytorch#145376); (4) re-test with `NCCL_CUMEM_ENABLE` unset now that NCCL is 2.30.4; (5) check whether hangs correlate with an idle-then-burst traffic pattern (#42742). The pyspy signature has been completely consistent across every run regardless of every variable tested: `EngineCore` blocked waiting for a `shm_broadcast` response; N-1 workers already finished locally and parked in a device-sync; one rank either caught still genuinely inside a live collective or already idle having apparently sent its data successfully while the others never receive it — now refined further by the `_to_list`/`transfer_event.synchronize()` finding, which shows this isn't staggered but a simultaneous freeze across all 8 ranks. Real, confirmed operational improvements along the way (none of them fixes): `no-async-scheduling: true`, `NCCL_NET_GDR_LEVEL: PHB`, `--network=disable_rdzv_get`, and the CPU allocation fix documented above. Ruled out: concurrent memory pressure (`max-num-seqs`), prefix caching, request-abort/cancellation (observed once, but inconsistent with many spontaneous hangs with no preceding cancellation), the `10c75477b`/`48aa8d8d7` lost-notify fix on its own, `disable-custom-all-reduce` (#8410's mechanism, tested 2026-08-12), and every CXI/libfabric/NCCL environment variable tried short of `NCCL_MNNVL_ENABLE`/`NCCL_PXN_DISABLE` (untested, lower priority now). Libfabric's own logging is confirmed exhausted as a diagnostic avenue on this platform (`info`/`trace`/`debug` all show nothing during a live hang, likely because this build isn't compiled with debug support) — consistent with, though not proof of, the CUDA-deadlock lead above, since a blocked syscall wouldn't necessarily produce any transport-level log line at all.

  **RESOLVED, 2026-09-02/03.** Root cause: this deployment's venv was actually running **NCCL 2.28.9**, not the 2.30.4 pinned in `slurm-vllm-setup.sh` on 2026-08-05 specifically to fix the Nemotron-3-Ultra hang above (the exact same bug class, vLLM issue #46097). The pin was present in the script the whole time but positioned *before* several later `uv pip install` calls (flashinfer-jit-cache, DeepGEMM, UCCL-EP, NIXL, `humming-kernels`, HPC-OPS); `uv` re-resolves the full dependency tree on every install, torch itself pins `nvidia-nccl-cu12==2.28.9` exactly, so any one of those later steps silently reverted the pin with no warning. It genuinely worked for the 0.25.1-era Nemotron venv (built before those later script steps existed) but never took effect for the fresh 0.26.0 venv GLM-5.2 ran on — confirmed via a ground-truth check (`ncclGetVersion()` called directly on the actually-mapped `.so`, not the unreliable `NCCL_DEBUG` log banner or `torch.cuda.nccl.version()`, both of which can be stale/misleading — see `report_setup()`). Fix: moved the pin to the very end of `slurm-vllm-setup.sh`, rebuilt the venv, confirmed 2.30.4 actually loads. `logs/glm52q/20260903_000609` served 56 successful chat completions over ~25 minutes with zero stall signatures before a clean user abort — first fully healthy run in this investigation.

  Everything else chased in this entry — CUDA-graph-replay theory, JIT-compile proximity, `shm_broadcast.py` memory-fence auditing, CXI/libfabric tuning, the NCCL topology finding that "local GPU slot 3" is always this cluster's inter-node Slingshot bridge — was real, reproducible observation about this deployment's behavior, just not the cause. Lesson for next time: when a hang resembles a *previously fixed* issue, verify the fix's pin actually still holds (ground-truth check, not the setup script or a version-reporting API) before re-investigating tuning knobs.

  **Which config knobs picked up during this investigation are still justified, now the actual cause is fixed (reviewed 2026-09-03):** several `glm-5.2-743b-int4.yaml` settings were added while chasing the NCCL bug and never revisited once it was found. Checked each against this document's own record of what was actually proven, not assumed:

  - **`numa-bind: true` — remove, always was dead weight, unrelated to the NCCL bug.** Already proven above (see the `numa-bind` bullet earlier in this entry) to be structurally inert under `RayExecutorV2` — `configure_subprocess()` is never called from that code path on vLLM 0.26.0, so this flag has never once affected any worker process here, independent of NCCL version. No reason to keep it.
  - **`no-async-scheduling: true` — was proven to only change *which* hang signature you hit, not fix anything; safe to remove now the actual hang is gone.** This document's own words from the live investigation: "changes which failure mechanism you hit, not just cosmetically... does not fix the underlying hang." It was masking/delaying symptoms of the same NCCL bug now fixed at the root. Re-enabling vLLM's default async scheduling should be tested for throughput — there's no evidenced reason left to keep it disabled.
  - **`disable-custom-all-reduce: true` — explicitly tested and ruled out as affecting the hang either way; safe to revert for performance.** From the 2026-08-12 live-run entry above: "`disable-custom-all-reduce: true` tested — does NOT fix the hang... #8410's custom-all-reduce hang mechanism is now ruled out for this specific hang." Since vLLM's custom all-reduce kernel is a genuine perf optimisation over plain NCCL for intra-node TP, and disabling it was proven to have zero effect on the actual bug, worth re-enabling and confirming no regression.
  - **`compilation-config: '{"pass_config": {"fuse_allreduce_rms": false}}'` — likely inert for GLM-5.2 specifically, not an NCCL-hang workaround at all.** Per `knowledge-base.md`'s Compilation table, this pass-level toggle is "the correct lever for models whose allreduce is routed through vLLM's compile pass (`AllreduceFusionPass` — Qwen3.5, DeepSeek-V3.2), as opposed to `IVLLM_DISABLE_FLASHINFER`... for models like MiniMax-M3/GLM-5.2 that call the fused op directly, bypassing the compile pass entirely." By that same document's own analysis, GLM-5.2 doesn't go through the code path this flag targets — meaning it was likely never doing anything for this model, added defensively ("Multi-node compilation fix") rather than after being shown necessary here specifically. Worth testing removal; `IVLLM_DISABLE_FLASHINFER` (already relied on, and now made to work correctly against the patched vLLM 0.26.0 per 2026-09-03) is the flag actually documented as relevant to this model family.

  None of these four were ever shown to be *necessary* for GLM-5.2 specifically — three (`numa-bind`, `no-async-scheduling`, `disable-custom-all-reduce`) were explicitly tested and shown to not touch the real cause, and the fourth (`fuse_allreduce_rms`) targets a code path this model's own docs say it doesn't use. Recommend testing removal of all four together against the now-working NCCL 2.30.4 baseline, watching for both a regression (would mean one of them was doing something real that just wasn't understood) and a throughput improvement (the more likely outcome, since three of the four exist specifically to change or disable a default optimisation).

- **MiniMax-M3-FP8-dynamic under hybrid TP=4×DP=2 (2×4-GPU nodes) — blocked on a Triton kernel crash after clearing three separate, confirmed bugs.** `examples/minimax-m3.yaml`, 2026-08-07. Goal: get MiniMax-M3 (427B/26B-active MoE, MSA sparse attention, EAGLE3 speculative decoding) serving across 8 GPUs on 2 nodes. Still not working end-to-end, but the investigation surfaced and fixed/worked-around three genuinely separate, reusable bugs along the way — worth reading in full before re-attempting.

  **Investigation path** (logs under `logs/mmax3/`, timestamps below; see conversation history for full detail):
  1. Early attempts (`20260807_10*`) hit a startup feasibility `ValueError` (requested KV cache larger than available) and separately an `AssertionError: DeepEP v2 (ElasticBuffer) not available` when trying `all2all-backend: deepep_v2` — the installed `deep_ep` is UCCL-EP's compatibility wrapper (`vendor/uccl`), confirmed to genuinely lack the v2 `ElasticBuffer` API, not a version-string mismatch. Settled on `all2all-backend: allgather_reducescatter` instead, which supports all quantization types.
  2. A GPU OOM during vLLM's flashinfer-autotune warmup dummy-run (`kernel_warmup.py` → `flashinfer_autotune` → `_dummy_run`) went **completely undetected by ivllm** — the process tree wedged (an exception inside `worker_busy_loop` was caught internally by vLLM and didn't kill the process) rather than crashing cleanly, so `wait_report()`'s death-detection never fired, and `monitor_head()` had no active liveness check during `initialising` at all (only during `running`, via idle-timeout log-grepping) — the job sat there until manually cancelled, and since `tidy_up()`'s `SIGUSR2`/201 (user-cancel) path never calls `capture_job_diagnostics()`, nothing was even auto-archived. Fixed two real ivllm gaps as a result: added `IVLLM_CRASH_INDICATORS` (`torch.OutOfMemoryError`, `EngineDeadError`, `WorkerProc hit an exception`) log-message detection to `monitor_head()`, and — after a first version placed that check *after* the `initialising` branch, where it was structurally unreachable since every path through that branch ends in `continue`/`return` — relocated it to run unconditionally near the top of the loop, before the status-specific branches. Confirmed working in `20260807_140029`: crash detected and diagnostics auto-archived while still `initialising`, no manual intervention needed.
  3. The OOM itself kept recurring across many gpu-memory-utilization/max-model-len tuning attempts despite `enable-flashinfer-autotune: false` being set, because **that setting was never actually taking effect** — root-caused to a separate vLLM bug (see "vLLM's `--config <file>.yaml` loader silently drops boolean `false` values" above in this same list) that silently drops any bare `key: false` yaml line for a flag whose real default isn't already `False`. Worked around by wrapping it as JSON instead: `kernel-config: '{"enable_flashinfer_autotune": false}'`. Confirmed fixed in `20260807_140029` (`enable_flashinfer_autotune=False` in the effective config dump) — the autotune-warmup OOM has not recurred since.
  4. With the OOM gone, hit `ValueError: Flashinfer allreduce is not supported for multi-node allreduce with 'trtllm' backend` — `vllm-env.sh:77` hardcodes `VLLM_FLASHINFER_ALLREDUCE_BACKEND=trtllm` for every job (comment: "Force standard NCCL for Slingshot stability"). Traced to `get_fi_ar_workspace()` (`vllm/distributed/device_communicators/flashinfer_all_reduce.py`) checking the *whole deployment's* node count (`get_node_count()`) instead of the specific TP group's own node span, even though it's already passed that exact group (`group=get_tp_group().device_group`, from `fused_allreduce_gemma_rms_norm.py`) — confirmed via `vendor/vllm` source and cross-checked against real logs from both this model and `qwen35-397` (same hybrid TP-node-local/PP-cross-node shape, same crash). Also confirmed via `vendor/vllm` that `deepseek_v32/nvidia/fused_ops.py` and `minimax_m3/amd/model.py` use the same fused op, so this affects those too. **Reported upstream to vllm-project/vllm.**
  5. Tried `VLLM_FLASHINFER_ALLREDUCE_BACKEND=mnnvl` as a workaround (routes around the buggy guard, which only fires for `backend=="trtllm"`) — this cleared the `ValueError`, but crashed harder: a genuine CUDA context failure, `CUDA error: an illegal memory access was encountered` from `ProcessGroupNCCL`'s watchdog thread across all ranks (`20260807_140029`'s follow-up, `20260807_145652`). Confirms `mnnvl` is genuinely unsafe on Isambard's Slingshot fabric (no real inter-node NVLink/NVSwitch for its multicast workspace) — not just unsupported, actually hardware-incompatible — which is almost certainly *why* `trtllm` was hardcoded as the default in the first place.
  6. Patched the installed vLLM directly instead: `design/prototype/patch/hybrid-trtllm.sh` changes `get_fi_ar_workspace()`'s guard to `_node_count(get_tp_group().cpu_group) > 1` — using vLLM's own existing `_node_count()` helper, scoped to the TP group instead of the whole deployment (must use `.cpu_group`, not `.device_group`; `_node_count()`/`in_the_same_node_as()` assert a non-NCCL group). See `design/prototype/patch/README.md` for full usage/caveats. This worked: `20260807_151912` shows `trtllm` selected with no crash, full engine init (27-37s), `/health` up, and the warmup request running real inference far enough to JIT-compile a long chain of MiniMax-M3's custom kernels.
  7. New crash, further in than anything before: `RuntimeError: Triton Error [CUDA]: an illegal memory access was encountered` inside MiniMax-M3's own sparse-attention decode kernel (`vllm/models/minimax_m3/common/ops/sparse_attn.py:511`, `_gqa_sparse_decode_kernel`), failing specifically in Triton's `_init_handles()` → `driver.active.utils.load_binary()` — i.e. while loading the *compiled kernel binary* into the CUDA context, not inside the kernel's own compute logic. That's more consistent with an already-corrupted CUDA context than a plain indexing bug in this one kernel, but not conclusive — only DP0's ranks show it in the one log captured (`20260807_151912`), which is mildly odd for a deterministic kernel bug but could just be incomplete capture.

  8. Drafted a second, complementary patch — `design/prototype/patch/diffs/skip-flashinfer-fusion-multinode.v0.25.1.patch` + generic `apply-vllm-patch.sh` tool — making `_can_use_flashinfer()` return `False` immediately whenever the TP group spans >1 node, so a genuinely cross-node TP group (TP=8×Ray, no DP split) never attempts either flashinfer backend at all and always takes the safe unfused fallback. See `design/prototype/patch/README.md`.
  9. Tested TP=8×Ray with patch (8) applied, using the **official** `MiniMaxAI/MiniMax-M3-MXFP8` checkpoint (ruling out the fp8-dynamic-vs-MXFP8 checkpoint format as a variable too). **Confirms step 7 was independent of allreduce backend**: the traceback shows the crash happening inside the patched, safe fallback path itself (`fused_allreduce_gemma_rms_norm.py:152` → plain `tensor_model_parallel_all_reduce` → `ncclAllReduce` → `RuntimeError: NCCL error: unhandled cuda error`) — flashinfer/trtllm/mnnvl were never touched. Combined with step 7's Triton-kernel-load crash and step 5's NCCL-watchdog crash, the same illegal-memory-access family has now surfaced through three unrelated call sites across three configurations (fp8-dynamic hybrid+trtllm-patch, fp8-dynamic hybrid+mnnvl, official-MXFP8 TP8×Ray+patch-8). Root cause is very likely a genuine bug in one of MiniMax-M3's own custom Triton kernels (sparse-attention/indexer/eagle — all JIT-compiling in the seconds before every one of these crashes), corrupting the CUDA context; whichever CUDA/NCCL call runs next just surfaces the resulting garbage state. No longer considered ambiguous — this is independent of allreduce backend, DP/Ray topology, and checkpoint quantization format.
  10. New, separate `monitor_head()` gap found while diagnosing (9): the crash-indicator check (added in step 2) is correctly positioned at the top of the loop, but once execution enters the `initialising` branch's warmup-request-sending code it stays inside that nested retry loop (5 attempts, `sleep 10` between) and never returns to the top of the outer loop to re-check — so a crash occurring *during* an in-flight warmup request is invisible to it. In (9) this meant detection fell back to the slower path (vLLM's own ~5-minute internal RPC-timeout self-shutdown, then `wait_report()` catching the resulting exit-code-0 process death — which `tidy_up()` correctly flags as anomalous, hence "shutdown was not particularly clean" but diagnostics were still captured). This is worked around but depends on the warmup curl requests timing out, and hence overall warm up failure so takes a long time to detect the crash.

  11. Root-caused a plausible upstream explanation for the step 7/9 crash family: found `vllm-project/vllm` issue [#48603](https://github.com/vllm-project/vllm/issues/48603) — "MiniMax M3 Triton path misinterprets token-major top-k buffer" — reporting the exact same `RuntimeError: Triton Error [CUDA]: an illegal memory access was encountered` signature, in the same kernel family (`common/indexer.py`, `common/ops/index_topk.py`, `_topk_index_kernel`). Root cause per the issue: `topk_indices_buffer` is allocated token-major (`[tokens, heads, topk]`) but the Triton indexer/sparse-attention kernels read it head-major. The actual merged fix is PR [#49149](https://github.com/vllm-project/vllm/pull/49149) (merged 2026-07-25, commit `d1a8ba63d9d2bb51ebf60dd5ea1463cf61c70cea`) — an earlier attempt, PR #48604, was closed unmerged/needs-rebase. Confirmed directly against source: **v0.25.1** (tagged 2026-07-12) predates the fix; **v0.26.0** (released 2026-07-27) has it — `nvidia/model.py` there allocates the buffer token-major with an explicit comment matching the fix description.
  12. Upgraded to v0.26.0 (fresh venv via `slurm-vllm-setup.sh`). Hit an unrelated, optional-dependency build failure along the way — NIXL's `python3 -m build --no-isolation` step fails its own "Getting build dependencies for wheel" check because `torch==2.11.*` declares an exact pin on `nvidia-nccl-cu12==2.28.9`, which this project deliberately overrides to `2.30.4` earlier in the same script (the Nemotron NCCL fix). NIXL doesn't actually use NCCL at all (UCX/libfabric-based), so this is a pure metadata-check collision; fix is `--skip-dependency-check`/`-x` on that `python -m build` call (not yet applied — skipped NIXL/humming/hpc-ops for this install, none of which the current configs use anyway). Then hit a **second, unrelated bug**: some early logs after the upgrade showed the driver process (`EngineCore`) running under `/vllm/0.25.1/...` while Ray worker actors ran under `/vllm/0.26.0/...` — two different vLLM installs mixed within the same job. Root-caused to the `ray-run-vllm.sh` variable-ordering bug (see its own `Known Issues` entry above) — fixed by hoisting the `IVLLM_JOB=${1:?...}` assignment to the top of the script; confirmed fixed in all subsequent runs (`grep -o "vllm/0\.2[56]\.[01]"` shows only one version per log from `20260807_191038` onward).
  13. With 0.26.0 + correct version resolution, re-applied `hybrid-trtllm.sh`'s node-count-scoped patch and tried a *genuinely* cross-node TP=8×Ray config (no DP split at all, unlike steps 4-9's TP=4×DP=2 hybrid). Hit the *original* `trtllm` `ValueError` again — but this time correctly, not a bug: for TP=8 with no DP, the TP group itself genuinely spans both nodes, so even the *patched* `_node_count(group) > 1` check correctly rejects `trtllm` (it fundamentally cannot do multi-node allreduce; the patch only fixes the case where the TP group is node-local but the deployment isn't). Confirmed via source this is unrelated to Ray or topology generally — Nemotron-3-Ultra runs TP=8×Ray across 2 nodes fine because its model code never calls `fused_allreduce_gemma_rms_norm`/`get_fi_ar_workspace` at all (no such log line ever appears in its logs); the guard is simply unreachable code for that model.
  14. Since genuine cross-node TP has no working flashinfer backend at all (`trtllm` correctly refuses it, `mnnvl` crashes the CUDA context on Slingshot per step 5), drafted a third, simpler patch: `design/prototype/patch/diffs/disable-flashinfer-env.v0.25.1.patch` — adds an `IVLLM_DISABLE_FLASHINFER=1` env-var check to the *top* of `_can_use_flashinfer()` (`fused_allreduce_gemma_rms_norm.py`), unconditionally routing to the existing safe fallback (`tensor_model_parallel_all_reduce` + plain `GemmaRMSNorm`) regardless of node topology. Opt-in per job via the yaml `env:` block, so it doesn't affect other models/jobs sharing the venv. Verified byte-exact apply/revert against `vendor/vllm`. Confirmed firing correctly in `20260807_191731`'s log (`"IVLLM_DISABLE_FLASHINFER=1 — skipping flashinfer fused allreduce+RMSNorm"`), and again after being separately re-applied to the fresh 0.26.0 venv (patches must be re-run per version — they don't carry over between installs).
  15. With flashinfer fully bypassed, hit a **different failure**: a plain `TimeoutError: RPC call to sample_tokens timed out` (no crash signature at all) at `multiproc_executor.py`'s default `VLLM_EXECUTE_MODEL_TIMEOUT_SECONDS=300`. Suspected stale JIT cache; deleted it and re-ran with `IVLLM_DEBUG_LEVEL: 2`'s pyspy stack-dump instrumentation enabled (`debug/pyspy-node*.log`, `20260807_202912`).
  16. pyspy traces showed **no deadlock** — every worker was legitimately busy the whole time (weight loading → tokenizer init → Triton JIT compilation), just slower than the 300s default. The most telling frames, right before the timeout: multiple node1 workers' `MainThread` stuck in `subprocess._try_wait`/`_posix_spawn`, called from `execute_model` → `minimax_m3_sparse_attn` → Triton's `make_cubin`/`compile_module_from_src` — i.e. the model's actual **first real forward pass** (not weight loading) hit a cold, never-before-compiled Triton kernel and was shelling out to `ptxas`/a C compiler as real subprocesses, with up to 4 GPU workers per node doing this concurrently and contending for the same CPU cores.
  17. Checked where these compiles write to: `TRITON_CACHE_DIR=/local/user/<uid>/<hostname>/<job>/triton` — correctly node-scoped (RAM-backed tmpfs, not a slow network filesystem), but **shared across all GPU workers co-located on that node** (not scoped per-rank/PID), consistent with the contention seen in (16). Separately, while investigating *why* caching had previously been characterized as "a source of instability" (possibly node-specific), found the JIT-cache-save/restore node-mismatch bug documented in its own `Known Issues` entry above — a real, independent structural risk, though not confirmed as the direct cause of any one crash here (the specific compilations chased in this investigation happen *after* `save_cache()`'s capture point and are never persisted either way).
  18. Bumped `VLLM_EXECUTE_MODEL_TIMEOUT_SECONDS` to 1800 (distinct from the already-set `VLLM_ENGINE_ITERATION_TIMEOUT_S=1800`) to rule out "just needs more time." Result: a **genuine crash** surfaced at ~5 minutes anyway, well inside the extended budget — `RuntimeError: CUDA error: CUBLAS_STATUS_EXECUTION_FAILED when calling cublasSgemm(...)`, this time from `vllm/model_executor/layers/fused_moe/router/gate_linear.py`'s `fp32_router_gemm_dispatch` — a plain `torch.nn.functional.linear(x.float(), weight)` call in the MoE router's gating layer (`20260807_205855`). As mundane an op as exists in the whole model; consistent with an already-corrupted CUDA context surfacing on an unrelated subsequent call, same pattern as steps 5, 7, and 9's three *different* call sites. No matching upstream issue found this time (checked GitHub issues and this file's own commit history directly, now that `vendor/vllm` has full history — see step 19).
  19. Converted `vendor/vllm` from a shallow single-tag clone to a full clone (`git fetch --unshallow --tags`; 18.5k commits, 174 `v0.*` tags) and added `vendor/vllm-0.26.0` as a `git worktree` alongside it, sharing the same object store. Both versions are now available locally for direct `git log`/`git diff v0.25.1..v0.26.0 -- <path>`/`git blame`, without round-tripping through the GitHub API for every version-comparison question.
  20. Disabled `enable-expert-parallel` (pure TP=8×DP=1, no cross-rank expert dispatch at all) to rule out EP as the cause of (18). Still crashed, same illegal-memory-access family (`CUBLAS_STATUS_EXECUTION_FAILED` on one rank, `torch.AcceleratorError: illegal memory access` on another — `20260807_211733`) — **EP is now ruled out**. Notably harder this time: Ray's raylet reported the dead worker via `SYSTEM_ERROR` ("crashed unexpectedly due to SIGSEGV or another unexpected error"), an OS-level death rather than a caught Python/CUDA exception. Also notably *further*: this crash happened while the model was already successfully serving real traffic — 4 concurrent `chatcmpl-*` requests scheduled together with mixed prefill+decode in the same step — the furthest any attempt has reached (past load, past warmup, into actual concurrent serving) before failing.

  **Ruled out, confirmed independent of the remaining crash**: flashinfer/trtllm/mnnvl entirely (step 14's env-var bypass confirmed active, crash still occurs with the fused path never touched at all); the top-k buffer bug from step 11 (fixed via the 0.26.0 upgrade, confirmed present in source); the vLLM-version-mixing bug from step 12 (fixed, confirmed via version grep on all later logs); EAGLE3/MTP speculative decoding (disabled in `20260807_173048`, crashed anyway with `speculative_config=None` confirmed in the effective config); expert-parallel dispatch (step 20); insufficient compile-timeout budget alone as a full explanation (step 18 — real crash, not merely a slow-compile timeout, though step 16's contention finding is still real and separately worth keeping); node reuse / poisoned GPU state from a prior crash (reproduces across many distinct physical nodes — `nid010495`, `nid010775`, `nid010400`, `nid010573`, `nid010575`, `nid010613`, ...); the fp8-dynamic-vs-official-MXFP8 checkpoint format (step 9 used the official `MiniMaxAI/MiniMax-M3-MXFP8` checkpoint, crashed identically).

  **What's still standing**: a recurring, genuine CUDA memory-corruption bug somewhere in MiniMax-M3's own execution path that (a) has surfaced through at least four unrelated immediate call sites (Triton kernel-binary load, `ProcessGroupNCCL` watchdog, plain `ncclAllReduce`, plain `cublasSgemm`) rather than one consistent one, consistent with an asynchronous/sticky CUDA error being reported by whatever operation happens to run next rather than at its true origin; (b) has occurred both during warmup/first-forward-pass and during real concurrent serving; (c) is independent of every subsystem individually ruled out above.

  **Left to investigate**:
  - **Concurrency/batching as the trigger**: the furthest-reaching crash (step 20) needed 4 concurrent mixed prefill+decode requests to surface. Try single-request-at-a-time traffic (no concurrency) — if stable, this points at the batched/heterogeneous-shape execution path specifically (still most likely the sparse-attention/indexer kernels, or the MoE router, but now scoped to their *batched* handling rather than any single request's forward pass).
  - **`enforce-eager` / CUDA graphs — a real, not-yet-controlled variable**: every fp8-dynamic hybrid config (steps 1-9) explicitly set `enforce-eager: true`. The official-MXFP8 TP8×Ray configs (steps 13 onward) do not, and run with `enforce_eager=False` / `cudagraph_mode: FULL_AND_PIECEWISE` in their effective config — i.e. CUDA graph capture/replay is active. Given this exact project's Nemotron-3-Ultra saga (top of this section) root-caused an entire multi-node hang to CUDA-graph-replay collective desync, this is a plausible, well-motivated, and so-far-untested variable — worth forcing `enforce-eager: true` on the MXFP8 config and re-testing.
  - **Common suspect across every signature so far**: `vllm/models/minimax_m3/common/ops/sparse_attn.py` and `common/indexer.py` are implicated one way or another in nearly every crash traceback captured (steps 7, 16, and indirectly 9/18/20 given the timing right after these kernels JIT-compile). If the single-request test above doesn't clear it, consider whether there's a way to force the non-MSA (plain dense-attention fallback, if MiniMax-M3 has one) path to isolate this file specifically.
  - Enough evidence now exists (four distinct manifestation sites, comprehensive ruled-out list) to write a credible upstream report even without a pinpointed line — worth doing once the `enforce-eager`/concurrency variables are controlled for, so the report reflects the narrowest possible repro.

  Five confirmed, independently-useful fixes/finds landed along the way regardless of the still-open blocker: two `monitor_head()` gaps fixed (one more found, unfixed — step 10); one worked-around vLLM upstream bug (`--config` boolean-drop); two locally-patched vLLM upstream/limitation bugs (`hybrid-trtllm.sh`, `disable-flashinfer-env.v0.25.1.patch`, the latter superseding `skip-flashinfer-fusion-multinode.v0.25.1.patch` for this model); one identified-and-fixed real upstream bug via version upgrade (top-k buffer, step 11); two new general (non-MiniMax-M3-specific) ivllm bugs found and documented as their own `Known Issues` entries (`ray-run-vllm.sh` variable ordering, JIT-cache node-mismatch).

  **Status**: not working end-to-end. Pausing here — see "Left to investigate" above for the concrete next steps when this is picked back up.

- **`src/engine/lib/utils.sh:24` uses `[[ -v IVLLM_UTILS ]]` which fails on Bash < 4.2 (such as macOS default `/bin/bash` 3.2)**:
  Running the unit test suite via `bun test` invokes `tests/bash/run.sh unit` using `/bin/bash`. On macOS, `/bin/bash` is version 3.2.57, which does not support the `-v` unary operator in conditional expressions (`[[ -v VAR ]]`). This causes a syntax error:
  `utils.sh: line 24: conditional binary operator expected`
  `utils.sh: line 24: syntax error near 'IVLLM_UTILS'`
  Consequently, `tests/unit/bash-integration.test.ts` fails when run in local macOS development environments where default `/bin/bash` is used instead of a newer Bash version (e.g. from Homebrew or on Linux/Isambard where Bash 5+ is standard).
  **Suggested fix**: Replace `[[ -v IVLLM_UTILS ]]` with standard bash-compatible parameter expansion like `[[ -n "${IVLLM_UTILS:-}" ]]` or `[ -n "${IVLLM_UTILS+x}" ]`, or invoke bash via `/usr/bin/env bash` ensuring a modern Bash is in PATH.

## Resolved

- **`bun:test` cannot reliably exercise umask-dependent child-process behaviour** (testing-infrastructure finding, not a product bug — kept here for the record since it cost real investigation time and could recur). While building the `copyDirectory` permission tests above, a test asserting group-write via the `--rsync-path 'umask 002 && rsync'` mechanism *alone* (before the `chmod -R g+rwX` fix existed) consistently failed under `bun test` but passed when the identical code (same `SshRemoteOps` class, same fake-`ssh`-on-PATH fixture used in `tests/unit/SshRemoteOps.test.ts`, same `process.umask(0o022)` simulation) was run via plain `bun run` instead — and the `bun run` result matched real Isambard behaviour. Conclusion: Bun's test runner (`1.3.14`) does something that breaks umask inheritance from a test's `process.umask()` call down to a spawned child process; the exact internal mechanism wasn't identified further. This is no longer blocking anything — the shipped fix (explicit `chmod -R g+rwX`) is deterministic and untroubled by this, so all three tests in `tests/unit/SshRemoteOps.test.ts` pass reliably under `bun:test`. Documented here in case a *future* test ever needs to depend on umask propagating to a spawned child under `bun test` — that will hit the same wall.
